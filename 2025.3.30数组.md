# 数组方法

```js
let arr = Array.of(1,2,3,4)
let brr  =Array.from({length:5},(_,index)=>index+1)
arr.fill(0)
arr.join('?')
arr.copyWithin()//浅复制
push, pop,shift,unshift
arr.sort((a,b)=>a-b)//升序
arr.reverse()
arr.concat(arr2)//合并
arr.splice(index,deleteCount ,...items)
arr.slice(start,end)
arr.reduce((pre,cur,index,curarr)=>{},initial)//reduceRight()
arr.find(()=>{})//findIndex()
arr.includes()
arr.indexOf()//lastIndexOf
arr.every()
arr.some()
forEach,map
arr.filter(()=>{})
arr.flat()//扁平化
```

```
"A".charCodeAt(0) → 65
"ABC".toLowerCase() → "abc"
"abc".toUpperCase() → "ABC"
String.fromCharCode(65, 66)      // "AB"
```
