## 面经

vue2，vue3的区别，vue3做了哪些性能优化

静态提升，diff优化，标记静态子节点

diff算法，虚拟dom，如何优化渲染

js,ts的区别

c,c++,js,ts区别，哪个是解释性语言，哪个是编译性语言，哪个效率更高

```
c,c++编译性语言：源代码需要先通过编译器转换为机器码，生成可执行文件后才能运行
ts：源代码通过TypeScript编译器转换为JavaScript代码，再由JavaScript引擎解释执行
js:解释性语言：源代码通常由浏览器或JavaScript引擎逐行解释执行
```

数组，链表区别,操作数据优劣快慢

堆栈区别

算法：力扣3.无重复字符的最长子串

```js
    var lengthOfLongestSubstring = function (s) {
      let cnt = 0;
      let arr = [];
      for (let i = 0; i < s.length; i++) {
        if (i === 0 || !arr.includes(s[i])) {
          arr.push(s[i]);
        } else {
          arr.shift();
          i--;
        }
        cnt = Math.max(cnt, arr.length);
      }
      return cnt
    };
```

