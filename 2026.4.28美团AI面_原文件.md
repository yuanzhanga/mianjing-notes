自我介绍，ai大模型怎么融入学习，怎么使用ai新工具

aicr中遇到有帮助的场景或者困难：幻觉，偷懒

http,https区别

https怎么做到安全性的，对称加密，非对称加密（公私）

两者通常是**结合使用**的：

1. **非对称加密**负责安全地交换"会话密钥"
2. **对称加密**负责后续大量数据的快速传输

js中作用域，作用域链的含义和使用场景

嵌套函数想访问外层函数的变量，而外层函数已经执行完毕，这时作用域链是怎么工作的 闭包

闭包可能导致内存泄露的情况有哪些，怎么处理的

事件委托的原理，有哪些应用场景

事件委托会失效或意外行为怎么解决



即时通讯系统前端页面，怎么设计消息存储和同步机制，以及用户切换设备后的消息同步问题

消息存储在浏览器缓存中，如何确保消息的安全性和隐私性

假如token被恶意窃取或泄露，怎么应对这种情况



前端封装一个'文本生成'SDK使用，设计调用参数与返回结构，至少包含输入，上下文，模型参数，输出结构，错误码以及用于追踪的id，说出前端如何支持流式返回包括回调协议，取消能力，以及同一页面多实例并发时如何隔离状态



做全链路可观测并打通一次请求的的traces,请说明前端如何生成或透传traceid,如何在一次对话中串联解锁耗时，重排耗时，模型耗时，token用量，工具调用耗时等指标的展示或上报，并说明前端侧会设置哪些体验告警阈值例如首包超时与长时间无增量



前端在生成traceid时会选择什么生成方式，如特定算法和规则

假如用户id不存在或者无法获取，前端如何确保生成的traceid仍然是唯一的

描述一次需要在有限资源下解决问题的经历，如何制定方案

