# 2026.8.28 百度二面

## 1. 后端学了些什么

笔记里实际做过/写过的：

- **Node.js + Express**：`bl-note/my-node-app`（express 5.1.0，GET / 返回 Hello Docker，listen 3000，配了 Dockerfile 部署）
- **JWT 认证接口**：jwt.sign 登录接口（expiresIn: 1h）、jwt.verify 校验中间件、Authorization 头传 token（计网.md JWT 段）
- **SSE / WebSocket / 流式**：express 写 SSE（res.writeHead + `text/event-stream`、消息格式 `event:/data:` + 双换行）、ws 库广播 + 心跳监测、chunked 分块传输（计网.md）
- **Next.js 服务端**：SSR/SSG/RSC、服务端渲染 + 水合（2026.5.26next.md）
- **学习计划中**：rust、nginx、RAG 嵌入/向量模型、TS 写工具调用 Agent 跑通第一个循环（bl-note/1.txt）

来源：`bl-note/my-node-app/`、`mianjing-notes/计网.md`、`2026.5.26next.md`、`bl-note/1.txt`

## 2. next 和 express 比有什么区别

| | Next.js | Express |
|---|---|---|
| 定位 | React **全栈应用框架** | Node.js 的 **HTTP 服务框架** |
| 渲染 | 支持 CSR / SSR / SSG / RSC，服务端渲染 HTML、客户端水合、局部水合、流式返回 | 只返回数据或静态内容，渲染交给前端 |
| 路由 | 文件系统路由约定（目录即路由） | 手动 `app.get/post` 逐个注册 |
| 内置能力 | 打包优化、图片/字体优化、SSG 静态导出 | 中间件体系（cors、express.json 等）按需挂 |
| 场景 | 需要 SEO、首屏快、前后端一体的 Web 应用 | 纯 API 服务、轻量后端 |

关键点：Next 把"渲染"从浏览器挪到了服务器（SSR 首屏快、SEO 好，但服务器压力大、开发成本高）；express 只是管道，数据 + 路由 + 中间件，渲染是前端的活。

来源：`2026.5.26next.md`（CSR/SSR/SSG/RSC 段）、`计网.md`（express 代码）

## 3. 部署上面有什么区别，怎么部署服务

- **静态站（SSG / CSR）**：`npm run build` → 生成静态文件 → 部署到 CDN / Nginx，无需服务器执行 JS，首屏快、压力小；不适合动态数据
- **SSR（Next）**：需要 Node 服务器常驻，每次请求服务端实时渲染 HTML，用户量大对服务器配置要求高
- **Express 服务**：node 进程监听端口，配合 nginx 反向代理对外暴露域名
- **自己项目（Docker 部署）**：`my-node-app/Dockerfile` 五步——`FROM node:22` → 先 `COPY package*.json` + `npm ci`（利用镜像层缓存）→ `COPY .` → `EXPOSE 3000` → `CMD npm start`
- **开发阶段**：vite dev server 的 proxy 把 `/api` 代理到 `http://localhost:3000`，防后端跨域（工程化.md）

来源：`2026.5.26next.md`、`bl-note/my-node-app/Dockerfile`、`工程化.md`

## 4. 对 docker 的理解

**笔记里没有 docker 理论文档**（bl-note/1.txt 只把它列为学习项），下面按标准知识 + 你自己的 Dockerfile 写：

- **镜像 / 容器**：Dockerfile 定义构建步骤，镜像 = 只读模板，容器 = 镜像的运行实例。你的 Dockerfile：node:22 基础镜像 → 拷贝 package.json 先 `npm ci` 装依赖（利用缓存层）→ 拷贝代码 → `EXPOSE 3000` → `CMD ["npm", "start"]`
- **沙盒是隔离的吗**：是。Linux namespace 隔离进程、网络、文件系统、用户；cgroup 限制 CPU/内存。但和虚拟机不同——**共享宿主内核**，所以比 VM 轻
- **和宿主可以通信吗**：可以。默认 bridge 网络下容器有自己的网络栈，但通过**端口映射**互通：`docker run -p 8080:3000` 把宿主机 8080 映射到容器 3000。注意 Dockerfile 里的 `EXPOSE 3000` 只是声明，真正暴露靠 `-p`
- **怎么暴露到域名上**：nginx 反向代理监听 80/443 → `proxy_pass` 转发到宿主机端口 → 再映射进容器端口

## 5. 域名是怎么到服务器的

计网.md（浏览器输入 url 发生什么）里的链路：

1. **DNS 查询**：先查浏览器缓存 → 本地 hosts 文件 → 都没有再去 DNS 服务器查（默认 UDP 53 端口，小包快）
2. DNS 返回域名对应的**服务器 IP**
3. 建立 **TCP 连接**（三次握手）
4. HTTPS 的话再 **TLS/SSL 握手**（交换证书、协商会话密钥）
5. 发送 **HTTP 请求报文**，服务器响应

来源：`计网.md`（浏览器输入url发生什么、DNS 段）

## 6. 实习过程中做过和 ai 相关的 — aireview

完整答案已写在 `2026.8.26字节一面.md` 第 6 题，要点：v1 远端 GitLab + GitHub Action MR 审查（幻觉、发现晚）→ v2 本地 MCP Server 两个 Tool（cr_shift_left_batch_review / cr_shift_left_post_batch_results）+ 规则引擎 + 大模型分层 + GitLab PAT 鉴权 → Skill 化优化（本地运行、免提 MR）→ 未解决问题：大变更分桶。

## 7. mcp 和 skill 有什么区别，为什么切换，webmcp

- **MCP**：工具调用协议。把能力封装成 Tool 注册给 Agent，Agent 像调本地函数一样调用。AICR v2 就是 MCP Server（stdio 启动，两个审查 Tool）。接入方式有 npx/全局/本地路径/Docker/Python/SSE/内嵌代码（AI.md MCP 段）
- **Skill**：提示词 + 脚本的知识包。输入关键词命中 skill，执行 skill 里的脚本（AI.md："输入 aicr 时，命中 skill 执行 skill 中的脚本，将 mcp 工具逻辑集成到脚本中"）
- **核心区别**：MCP 是"外部进程暴露工具给模型调"的标准协议，跨客户端通用；Skill 是"客户端侧的提示词+本地脚本"，更轻量、无独立进程
- **为什么切换**：Skill 把 MCP 工具逻辑集成进脚本后**本地运行**，不需要重复提交 MR，也不依赖 MCP Server 进程
- **webmcp**：笔记没有记录。标准知识：浏览器端的 MCP——通过浏览器插件把网页/浏览器能力暴露成 MCP 工具，让 Agent 能操作浏览器上下文

来源：`AI.md`（AICR 优化段、MCP 段）、`2026.8.20百度一面.md`（functioncall 为什么不用 skill）

## 8. aicoding 实现一个全栈项目，实现一个 todolist 的增删改查

笔记里没有 todolist 记录，但有可套用的架子：

- **ai 搭建流程**（AI.md 心得）：新项目搭建先让 AI 输出目录结构 → 画流程图 → 写伪代码 → **确认后再生成具体文件**
- **后端模式**（计网.md 的 express 模式）：`app.use(cors())` + `app.use(express.json())` → 路由 `app.get/post/put/delete('/api/todos')` → 内存数组或 JSON 文件存储 → `app.listen(3000)`
- **前端**：列表渲染 + 表单提交，fetch 调接口
- **部署**：套 `my-node-app/Dockerfile` 那套（FROM node:22 → npm ci → EXPOSE → CMD）
- **回答话术**：先让 AI 出目录结构和接口设计（GET/POST/PUT/DELETE /api/todos），确认后生成代码，本地跑通，最后 Docker 化部署

## 9. openspec

**笔记里只有一处线索**（AI.md Prompt Engineering 段）："ReAct（Reasoning + Acting），让 AI 先思考再行动，openspec"；2026.8.25 快手一面问了"为什么要使用 openspec、openspec 和 superpower 的区别"（没记答案）。

标准知识补全：

- **openspec = 规格驱动开发**：让 AI 先产出可审阅的规范文档（需求、设计、任务清单），人确认后才动手写代码，之后按规范实现、按规范验收
- **为什么用**：让 AI "先思考再行动"——直接生成代码容易跑偏、无法审阅、不可追踪；先有 spec 就能在写代码前纠正方向，spec 本身可复用、可评审
- **和 AI.md 心得的对应**：就是"先输出目录结构、画流程图、写伪代码，确认后再生成具体文件"的正式化版本

## 10. agent.md / claude.md / codex.md 文件是干嘛的，应该写一些什么

笔记没有直接记录，但 AI.md 里的 `.cursorrules` 是同类物（"声明项目背景、用例模板、API 模式、命名规范等 → 全局生效，团队共享，强约束，适合标准化项目"）。

- **作用**：给 AI 编码工具的项目说明书，工具启动时自动加载进上下文，让 AI 每次对话都带着项目规则
- **写什么**：项目背景、技术栈、常用命令、目录结构、代码规范、命名约定、注意事项
- **区别**：AGENTS.md 是通用标准（多工具都读）；CLAUDE.md 是 Claude Code 专用；Codex 读 AGENTS.md / CODEOWNERS 类似的约定
- **你自己就有实例**：`~/.claude/CLAUDE.md`（个人档案 + 全局规范），写的就是"我是谁、项目组、去哪查工具规范"这类信息
