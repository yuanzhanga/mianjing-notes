

#### 常用的数据类型

原始类型  栈

number,string,boolean,undefined,null,symbol,**bigint**

引用类型  堆

object,array,function,date,正则,map/set/weakmap/weakset,error

#### 怎么判断类型

**typeof 看原始类型，Array.isArray 看数组，Object.prototype.toString.call 看一切，instanceof 看原型继承关系。最稳妥的通用方案是 toString.call**

注：type of null=object;type of []=object

#### 数组链表区别

操作数据优劣快慢

数组用**空间连续性**换**访问速度**；链表用**访问速度**换**插入灵活性**

#### 堆和栈的区别

| 维度         | 栈（Stack）                          | 堆（Heap）                              |
| ------------ | ------------------------------------ | --------------------------------------- |
| **管理方式** | 系统自动分配/释放                    | 程序员手动申请，需主动释放（或依赖 GC） |
| **存储内容** | 局部变量、函数参数、返回地址、值类型 | 对象实例、引用类型的数据、动态分配内存  |
| **数据结构** | 后进先出（LIFO）的线性结构           | 树形/链式的非线性结构                   |
| **访问速度** | 极快（CPU 直接操作栈指针）           | 较慢（需寻址，可能涉及内存碎片）        |
| **内存大小** | 很小（通常 1~8 MB，线程独立）        | 很大（受限于系统可用内存）              |
| **生命周期** | 随函数调用开始/结束自动管理          | 不主动释放则长期存在                    |
| **碎片问题** | 无（连续压栈/弹栈）                  | 有（频繁分配释放产生碎片）              |
| **线程安全** | 线程私有，天然安全                   | 多线程共享，需同步机制                  |

#### 排序算法

**1. 冒泡排序**

```javascript
function bubbleSort(arr) {
    const n = arr.length;
    for (let i = 0; i < n - 1; i++) {
        let swapped = false;
        for (let j = 0; j < n - 1 - i; j++) {
            if (arr[j] > arr[j + 1]) {
                [arr[j], arr[j + 1]] = [arr[j + 1], arr[j]];
                swapped = true;
            }
        }
        if (!swapped) break;  // 已有序，提前退出
    }
    return arr;
}
```

- **最好 O(n)**：已有序，只需一轮比较
- **最坏 O(n²)**：逆序

**2. 快速排序**

```javascript
function quickSort(arr) {
    if (arr.length <= 1) return arr;
    const pivot = arr[Math.floor(arr.length / 2)];
    const left = arr.filter(x => x < pivot);
    const middle = arr.filter(x => x === pivot);
    const right = arr.filter(x => x > pivot);
    return [...quickSort(left), ...middle, ...quickSort(right)];
}
```

- **平均 O(n log n)**：每次大致平分
- **最坏 O(n²)**： pivot 选到最大/最小值（可用随机化避免）

```js
//双指针法
function partition(arr, left, right) {
    const pivot = arr[right];  // 选最右边为基准
    let i = left;            // i 指向 < pivot 的区域的下一个位置
    
    for (let j = left; j < right; j++) {
        if (arr[j] < pivot) {
            [arr[i], arr[j]] = [arr[j], arr[i]];
            i++;
        }
    }
    
    // pivot 放到中间
    [arr[i], arr[right]] = [arr[right], arr[i]];
    return i;
}
```

**3. 归并排序**

```javascript
function mergeSort(arr) {
    if (arr.length <= 1) return arr;
    const mid = Math.floor(arr.length / 2);
    const left = mergeSort(arr.slice(0, mid));
    const right = mergeSort(arr.slice(mid));
    return merge(left, right);
}
function merge(left, right) {
    let i = 0, j = 0;
    const res = [];
    while (i < left.length && j < right.length) {
        if (left[i] < right[j]) {
            res.push(left[i++]);
        } else {
            res.push(right[j++]);
        }
    }
    res.push(...left.slice(i));
    res.push(...right.slice(j));
    return res;
}
```

- **稳定 O(n log n)**：始终二分，无最坏情况
- **需要 O(n) 额外空间**

**4. 堆排序**

```javascript
function heapSort(arr) {
    // 建堆 + 逐个弹出堆顶
    const heap = [...arr];
    buildHeap(heap);
    const result = [];
    while (heap.length > 0) {
        result.push(popHeap(heap));
    }
    return result;
}
```

- **O(n log n) 稳定**，**O(1) 额外空间**

**5.选择排序**

**思路**：每次从未排序部分找最小元素，放到已排序末尾

```javascript
function selectionSort(arr) {
    const n = arr.length;
    for (let i = 0; i < n - 1; i++) {
        let minIdx = i;
        for (let j = i + 1; j < n; j++) {
            if (arr[j] < arr[minIdx]) {
                minIdx = j;
            }
        }
        // 交换到已排序末尾
        [arr[i], arr[minIdx]] = [arr[minIdx], arr[i]];
    }
    return arr;
}
```

**6.插入排序**

**思路**：逐个取出元素，插入到已排序部分的正确位置

```javascript
function insertionSort(arr) {
    const n = arr.length;
    for (let i = 1; i < n; i++) {
        const key = arr[i];
        let j = i - 1;
        // 从后往前找插入位置
        while (j >= 0 && arr[j] > key) {
            arr[j + 1] = arr[j];  // 后移
            j--;
        }
        arr[j + 1] = key;
    }
    return arr;
}
```