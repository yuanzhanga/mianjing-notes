meta标签

seo,字符编码，视口设置，社交分享，性能与安全，PWA 相关

BFC

margin塌陷，清除浮动

Dom事件怎么传递

事件代理

怎么组织默认冒泡

animation介绍一些属性和配置

线性函数，径向函数的属性 easy easy-in easy-in-out 贝塞尔

event loop js是一门单线程语言，任务产生的微任务会继续优先执行

```
const arr =[ 1 , 2 ,0, undefined, null ,'bbb','aaa',1,'aaa']
对数组进行去重
不用set/用set
```

状态码

206 

301永久重定向 302临时重定向 304命中缓存

401没有权限 403禁止访问 405方法错误

500 502网关层错误

vue2,3响应式实现 vue2对数组方法进行重写

怎么开启一个多线程，不支持webworker的降级方案是什么

时间切片，异步隔离

权限系统怎么实现，数据库有哪些表，三个t表，一个r表

防止数据库注入，字符转义，在dao层操作数据库

怎么处理上下文过爆，注意力飘逸的情况怎么处理

什么是STD

spack to first 

TDD

spack drive develment

ai生成的结果不符合预期，怎么处理上下文被污染

上下文过爆用"分层记忆 + RAG + 摘要压缩"解决；注意力飘逸用"每轮锚定约束 + 结构化输出 + 离题检测"解决

mcp

codevis,getmr

远程mcp，本地skill cursorrule,eslint静态+大模型