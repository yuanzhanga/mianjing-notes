 ![image-20260830005042380](https://18403581474.oss-cn-beijing.aliyuncs.com/img/202608300050535.png)

![image-20260830005057085](https://18403581474.oss-cn-beijing.aliyuncs.com/img/202608300050190.png)

![image-20260830005122141](https://18403581474.oss-cn-beijing.aliyuncs.com/img/202608300051240.png)

![image-20260830005132731](https://18403581474.oss-cn-beijing.aliyuncs.com/img/202608300051857.png)

![image-20260830005201313](https://18403581474.oss-cn-beijing.aliyuncs.com/img/202608300052435.png)

![image-20260830005221478](https://18403581474.oss-cn-beijing.aliyuncs.com/img/202608300052579.png)

![image-20260830005235001](https://18403581474.oss-cn-beijing.aliyuncs.com/img/202608300052113.png)

![image-20260830005247400](https://18403581474.oss-cn-beijing.aliyuncs.com/img/202608300052507.png)

![image-20260830005941085](https://18403581474.oss-cn-beijing.aliyuncs.com/img/202608300059194.png)

```js
const rl = require("readline").createInterface({ input: process.stdin });
var iter = rl[Symbol.asyncIterator]();
const readline = async () => (await iter.next()).value;

void async function () {
    let T = parseInt(await readline())
    let out = []
    while(T--){
        let n = parseInt(await readline())
        let s = await readline()
        let ans = 0
        for(let c = 0;c<26;c++){
            let ch = String.fromCharCode(97+c)
            let cnt = 0
            let inSeg = false
            for(let i = 0;i<n;i++){
                if(s[i]===ch){
                    if(!inSeg){
                        cnt++
                        inSeg = false
                    }
                } else if (s[i]<ch){
                    inSeg = false
                }
            }
            ans+=cnt
        }
        out.push(ans)
    }
    console.log(out.join('\n'))
}()
```







# Webhook事件投递与重试编排中枢

## AI Coding 做题说明

1. 【**关于题目**】请根据本文档的题目描述，完成题目要求的相关功能开发实现。
2. 【**关于分数**】本题采用**测试用例**与**对话过程**综合评分方式。题目具有一定挑战性，最终测试用例得分并不等同于本题最终成绩；评分还将综合考察考生与 AI 助手在任务分析、方案迭代和协作解决问题过程中的表现。
3. 【**预设代码**】本题提供系统预设代码；选择作答语言后，预设工程会自动初始化到工作区根目录。
4. 【**提交判分**】页面上方的「**提交判分**」按键，会使用隐藏用例对你的代码进行测试，并通过用例情况以及失败用例提示。注意，「提交判分」有次数限制。测试用例部分的得分，取**最高分**而不是最后一次得分。
5. 【**用例判分约定**】判分相关约定需要严格按照「服务启动要求」一节进行处理，否则会导致判分失败。
6. 【**编程语言选择**】可用作答语言：Python 3.11 / Go 1.22 / Java 21（Maven）/ JavaScript（Node.js 22）/ TypeScript（Node.js 22 + tsx）/ C++（g++ 12，C++17），详情参考「开发环境说明」一节。
7. 【**测试样例**】`./sample_testcases/run_sample_testcases.py` 为预设测试样例文件，点击上方的「试运行样例」按键可自动执行，也可通过终端命令手动执行。默认样例仅用于测试连通性，不代表最终得分。
8. 【**代码 diff 提示**】可通过左下方「时间线」的入口，查看不同时间节点的代码 diff。
9. 【**AI 使用提示**】当前模式下，AI 助手**不支持执行终端命令**，请手动执行命令，并将执行结果反馈给 AI。
10. 【**AI 使用提示**】当前 AI 助手为对话模式，当 AI 陷入僵局无法自我纠错时，重新启动一次新的对话，也许是一个更好的尝试。
11. 【**网络异常处理**】全部文件的编辑需实时同步到服务器。若遇到无法编辑、保存代码或 AI 助手无响应，请检查网络；异常持续时请联系客服。

## 需求背景

SaaS 平台的事件投递系统为平台运营方和租户集成方提供可靠的 Webhook 控制面，负责把订单、账单、工单等业务事件转换为可追踪的投递任务，并解决端点临时不可用、worker 失联和重复请求带来的一致性问题。

1. **配置与发布**：租户配置端点和订阅，发布业务事件后生成对应的投递任务。
2. **调度与回报**：worker 按端点领取带租约的任务并回报投递结果，服务持续维护任务的可领取状态。
3. **失败恢复**：系统处理退避重试、生命周期到期、熔断和死信回放，并留下可查询的审计记录。

请交付一个常驻 HTTP 服务，完整提供上述控制面能力，并保证租户隔离、投递状态、额度和审计结果在连续请求及时间推进后保持一致。本题不访问真实 Webhook URL；全部时间行为由整数秒虚拟时钟驱动，不得使用真实墙钟或随机数。

## 本次任务重点

本题提供可运行的服务框架。你需要根据下文契约完成事件投递编排服务：

1. **实现配置与事件扇出**：维护租户、端点和订阅，并为匹配的订阅生成投递任务。
2. **处理投递生命周期**：完成领取、租约、结果回报、重试、到期、熔断和死信回放流程。
3. **校验写入一致性**：拒绝非法或冲突请求，保证失败操作无副作用，并正确处理幂等重放。
4. **维护确定性状态**：保证租户隔离、虚拟时钟、并发额度、顺序约束和审计序列一致。

> 所有查询结果、实体状态和审计记录都应反映同一份确定性的业务事实。

## 实体与字段

> 下表定义各实体在 HTTP API 中公开的字段。字段名、类型或格式、单位、必填或可选、默认值、枚举取值以及读写属性均属于接口契约，必须按本文实现。请求传入字段、服务端生成或维护字段、派生只读字段的具体约束由字段表、请求字段约定和业务规则共同确定；内部存储方式不作要求。

**Tenant**：租户是数据、幂等键和审计序列的隔离边界。

| 字段       | 含义                               |
| ---------- | ---------------------------------- |
| id         | 服务端分配的租户 ID，如 `tenant_1` |
| name       | 非空租户名称                       |
| status     | 固定为 `ACTIVE`                    |
| created_at | 创建时虚拟时钟秒数                 |

**Endpoint**：接收投递的逻辑端点及其当前运行策略。

| 字段               | 含义                                |
| ------------------ | ----------------------------------- |
| id                 | 端点 ID，如 `endpoint_1`            |
| tenant_id          | 所属租户                            |
| name               | 非空端点名称                        |
| status             | `ACTIVE` / `PAUSED`                 |
| max_inflight       | 端点允许的同时有效租约数            |
| lease_seconds      | 单次租约时长，单位秒                |
| failure_threshold  | CLOSED 状态下触发熔断的连续失败次数 |
| cooldown_seconds   | OPEN 持续时间，单位秒               |
| max_attempts       | 单个 Delivery 最多可被领取的次数    |
| base_delay_seconds | 指数退避基础时长，单位秒            |
| max_delay_seconds  | 单次退避上限，单位秒                |
| event_ttl_seconds  | 新 Delivery 的生命周期，单位秒      |
| circuit_state      | `CLOSED` / `OPEN` / `HALF_OPEN`     |
| failure_streak     | 端点当前连续失败次数                |
| open_until         | OPEN 结束时刻；其他状态为 `null`    |
| created_at         | 创建时虚拟时钟秒数                  |

**Subscription**：事件类型到端点的订阅关系。

| 字段        | 含义                                     |
| ----------- | ---------------------------------------- |
| id          | 订阅 ID，如 `subscription_1`             |
| tenant_id   | 所属租户                                 |
| endpoint_id | 目标端点，必须属于同一租户               |
| event_types | 非空字符串数组；`*` 表示匹配全部事件类型 |
| status      | `ENABLED` / `DISABLED`                   |
| created_at  | 创建时虚拟时钟秒数                       |

**Event**：租户发布的一次不可变业务事件。

| 字段            | 含义                                  |
| --------------- | ------------------------------------- |
| id              | 事件 ID，如 `event_1`                 |
| tenant_id       | 所属租户                              |
| event_type      | 非空事件类型                          |
| payload         | 任意 JSON 对象                        |
| ordering_key    | 可空字符串；非空时启用同 key 顺序约束 |
| idempotency_key | 租户内事件发布幂等键                  |
| created_at      | 发布时间                              |
| delivery_ids    | 本次发布生成的 Delivery ID 数组       |

**Delivery**：一个 Event 对一个 Subscription 生成的投递任务。

| 字段             | 含义                                                         |
| ---------------- | ------------------------------------------------------------ |
| id               | 投递 ID，如 `delivery_1`                                     |
| tenant_id        | 所属租户                                                     |
| event_id         | 来源事件                                                     |
| subscription_id  | 来源订阅                                                     |
| endpoint_id      | 目标端点                                                     |
| ordering_key     | 来源事件的顺序键                                             |
| status           | `PENDING` / `LEASED` / `RETRY_WAIT` / `SUCCEEDED` / `DEAD`   |
| attempt_count    | 已成功领取租约的次数                                         |
| available_at     | 最早允许再次领取的时刻                                       |
| lease_token      | 当前有效租约 token；非 LEASED 时为 `null`                    |
| lease_expires_at | 当前租约到期时刻；非 LEASED 时为 `null`                      |
| expires_at       | Delivery 的 TTL 到期时刻                                     |
| last_result      | `SUCCESS` / `RETRYABLE` / `PERMANENT` / `LEASE_TIMEOUT` / `EXPIRED` 或 `null` |
| policy           | 事件发布或死信回放时锁定的策略对象，固定包含 `lease_seconds`、`max_attempts`、`base_delay_seconds`、`max_delay_seconds`、`event_ttl_seconds` 五个正整数字段 |
| replay_of        | 回放来源 Delivery ID；普通任务为 `null`                      |
| created_at       | 创建时刻                                                     |

**DeadLetter**：Delivery 无法继续投递后生成的不可变记录。

| 字段                | 含义                                     |
| ------------------- | ---------------------------------------- |
| id                  | 死信 ID，如 `dead_1`                     |
| tenant_id           | 所属租户                                 |
| delivery_id         | 原 Delivery ID                           |
| endpoint_id         | 目标端点                                 |
| reason              | `PERMANENT` / `MAX_ATTEMPTS` / `EXPIRED` |
| created_at          | 进入死信的时刻                           |
| replay_delivery_ids | 由该死信创建的回放 Delivery ID 数组      |

**AuditEntry**：租户内按发生顺序追加的审计记录。

| 字段      | 含义                                 |
| --------- | ------------------------------------ |
| seq       | 租户内从 1 开始严格递增的序号        |
| at        | 记录产生时的虚拟时钟秒数             |
| type      | 稳定的动作类型字符串，取值见业务规则 |
| entity_id | 主要实体 ID                          |

## 接口说明

### 接口清单

| 方法 | 路径                                       | 功能                                               |
| ---- | ------------------------------------------ | -------------------------------------------------- |
| GET  | `/health`                                  | 服务健康探活                                       |
| POST | `/admin/reset`                             | 清空全部数据、幂等记录和审计，并把虚拟时钟恢复为 0 |
| GET  | `/clock`                                   | 查询当前虚拟时钟                                   |
| POST | `/clock/advance`                           | 按秒推进虚拟时钟并结算到期动作                     |
| POST | `/tenants`                                 | 创建租户                                           |
| GET  | `/tenants`                                 | 按 ID 数字后缀升序列出租户                         |
| GET  | `/tenants/{tenant_id}`                     | 查询租户                                           |
| POST | `/endpoints`                               | 创建端点                                           |
| GET  | `/endpoints`                               | 按筛选条件列出端点                                 |
| GET  | `/endpoints/{endpoint_id}`                 | 查询端点                                           |
| POST | `/endpoints/{endpoint_id}/pause`           | 暂停端点领取新任务                                 |
| POST | `/endpoints/{endpoint_id}/resume`          | 恢复端点领取新任务                                 |
| POST | `/endpoints/{endpoint_id}/policy`          | 更新端点当前策略                                   |
| POST | `/subscriptions`                           | 创建订阅                                           |
| GET  | `/subscriptions`                           | 按筛选条件列出订阅                                 |
| GET  | `/subscriptions/{subscription_id}`         | 查询订阅                                           |
| POST | `/subscriptions/{subscription_id}/disable` | 禁用订阅                                           |
| POST | `/subscriptions/{subscription_id}/enable`  | 启用订阅                                           |
| POST | `/events`                                  | 发布事件并生成投递任务                             |
| GET  | `/events`                                  | 按筛选条件列出事件                                 |
| GET  | `/events/{event_id}`                       | 查询事件                                           |
| POST | `/dispatch/claim`                          | worker 从指定端点领取投递租约                      |
| POST | `/deliveries/{delivery_id}/complete`       | 使用当前租约 token 回报投递结果                    |
| GET  | `/deliveries`                              | 按筛选条件列出投递任务                             |
| GET  | `/deliveries/{delivery_id}`                | 查询投递任务                                       |
| GET  | `/dead_letters`                            | 按筛选条件列出死信                                 |
| GET  | `/dead_letters/{dead_id}`                  | 查询死信                                           |
| POST | `/dead_letters/{dead_id}/replay`           | 从死信创建新的投递任务                             |
| GET  | `/audit`                                   | 查询指定租户的审计记录                             |

所有列表响应使用对象信封，例如 `{"tenants": [...]}`、`{"deliveries": [...]}`；审计查询固定返回 `{"audit": [...]}`。列表均按 ID 的数字后缀升序返回。创建 Tenant、Endpoint、Subscription、Event 的接口分别返回创建后的完整实体；暂停、恢复和策略更新返回完整 Endpoint，订阅启停返回完整 Subscription。完成回报和死信回放都返回操作后的完整 Delivery，幂等重放返回首次保存的同一响应快照。

## 业务规则说明

### 通用规则

- 全部时间均为非负整数秒，服务启动和 reset 后 `now = 0`。
- ID 由服务端按实体类型独立分配并递增；reset 后计数器归零。
- JSON 对象比较用于幂等冲突判断时采用结构相等，不依赖对象键顺序。
- 创建、状态变更和有效的任务流转会追加审计；同一租户的 `seq` 连续递增。
- Endpoint 暂停/恢复、Subscription 禁用/启用在实体已经处于目标状态时是成功的幂等 no-op：返回当前完整实体，不追加审计。Endpoint 策略更新按合并后的完整策略比较；提交值全部与当前值相同时同样返回当前实体且不追加审计。
- 发布事件时先追加 `EVENT_PUBLISHED`，再按 Subscription ID 顺序为本次扇出的每个 Delivery 追加 `DELIVERY_CREATED`。自动时间动作的审计 `at` 使用实际 TTL、租约或冷却边界时刻，而不是大跨度推进后的目标时刻。
- 参数缺失、类型错误、数值越界、目标不存在或状态不允许时，应拒绝操作，不得创建或改变任何业务数据，也不得使服务崩溃。具体 HTTP 状态码不作硬性要求。
- 返回实体时必须包含「实体与字段」列出的全部字段；可以增加辅助字段，但不得改变约定字段的类型和含义。
- AuditEntry.type 的稳定取值为：`TENANT_CREATED`、`ENDPOINT_CREATED`、`ENDPOINT_PAUSED`、`ENDPOINT_RESUMED`、`ENDPOINT_POLICY_UPDATED`、`SUBSCRIPTION_CREATED`、`SUBSCRIPTION_ENABLED`、`SUBSCRIPTION_DISABLED`、`EVENT_PUBLISHED`、`DELIVERY_CREATED`、`DELIVERY_CLAIMED`、`DELIVERY_RETRY_SCHEDULED`、`DELIVERY_SUCCEEDED`、`DELIVERY_DEAD`、`DEAD_LETTER_CREATED`、`DEAD_LETTER_REPLAYED`、`CIRCUIT_HALF_OPEN`。

### 状态枚举（精确字符串）

- **Tenant**：`ACTIVE`
- **Endpoint.status**：`ACTIVE` / `PAUSED`
- **Endpoint.circuit_state**：`CLOSED` / `OPEN` / `HALF_OPEN`
- **Subscription.status**：`ENABLED` / `DISABLED`
- **Delivery.status**：`PENDING` / `LEASED` / `RETRY_WAIT` / `SUCCEEDED` / `DEAD`
- **完成结果 result**：`SUCCESS` / `RETRYABLE` / `PERMANENT`
- **Delivery.last_result**：`SUCCESS` / `RETRYABLE` / `PERMANENT` / `LEASE_TIMEOUT` / `EXPIRED` / `null`
- **DeadLetter.reason**：`PERMANENT` / `MAX_ATTEMPTS` / `EXPIRED`

### 业务参数（硬约定，必须按此实现）

判分系统会严格按以下数值验证。题目仅约定数值，触发条件下的级联效果与状态流转请结合后续业务说明实现。

| 参数                    | 含义                      | 数值        |
| ----------------------- | ------------------------- | ----------- |
| 虚拟时钟初值            | 服务启动/reset 后的 `now` | **0 秒**    |
| 默认 max_inflight       | 创建 Endpoint 时缺省值    | **2**       |
| 默认 lease_seconds      | 创建 Endpoint 时缺省值    | **10 秒**   |
| 默认 failure_threshold  | 创建 Endpoint 时缺省值    | **3 次**    |
| 默认 cooldown_seconds   | 创建 Endpoint 时缺省值    | **30 秒**   |
| 默认 max_attempts       | 创建 Endpoint 时缺省值    | **4 次**    |
| 默认 base_delay_seconds | 创建 Endpoint 时缺省值    | **5 秒**    |
| 默认 max_delay_seconds  | 创建 Endpoint 时缺省值    | **60 秒**   |
| 默认 event_ttl_seconds  | 创建 Endpoint 时缺省值    | **300 秒**  |
| claim.limit 范围        | 单次领取数量              | **1 到 20** |

### 请求字段约定

- `POST /clock/advance`：`seconds` 必须为正整数；响应为 `{"now": N}`。
- `POST /tenants`：`name` 为非空字符串。
- `POST /endpoints`：必须提供 `tenant_id`、`name`；策略字段可选。所有策略字段必须是正整数，且 `max_delay_seconds >= base_delay_seconds`。
- `GET /endpoints`：支持可选查询参数 `tenant_id`。
- `POST /endpoints/{id}/policy`：只允许提交 `max_inflight`、`lease_seconds`、`failure_threshold`、`cooldown_seconds`、`max_attempts`、`base_delay_seconds`、`max_delay_seconds`、`event_ttl_seconds` 中的一个或多个；空对象非法，按合并后的完整策略做原子校验。
- `POST /subscriptions`：必须提供 `tenant_id`、`endpoint_id`、`event_types`；可选 `enabled`，缺省为 `true`，显式 `null` 非法。`event_types` 必须是非空字符串数组；重复值会自动去重并保留首次出现顺序。
- `GET /subscriptions`：支持可选查询参数 `tenant_id`、`endpoint_id`。
- `POST /events`：必须提供 `tenant_id`、`event_type`、`payload`、`idempotency_key`；`payload` 必须是 JSON 对象且允许空对象；`ordering_key` 可选，缺省为空字符串，显式 `null` 非法。
- `GET /events`：支持可选查询参数 `tenant_id`。
- `POST /dispatch/claim`：必须提供 `endpoint_id`、非空字符串 `worker_id`、`limit`。`worker_id` 只用于校验本次领取意图，不写入 Delivery 或响应。
- `POST /deliveries/{id}/complete`：必须提供非空字符串 `lease_token`、精确枚举 `result`、去除首尾空白后仍非空的字符串 `completion_key`；显式 `null` 和空白字符串非法。
- `GET /deliveries`：支持可选查询参数 `tenant_id`、`endpoint_id`、`event_id`、`status`。
- `GET /dead_letters`：支持可选查询参数 `tenant_id`、`endpoint_id`。
- `POST /dead_letters/{id}/replay`：必须提供去除首尾空白后仍非空的字符串 `idempotency_key`；回放意图只由 DeadLetter ID 和该 key 标识。
- `GET /audit`：必须提供 `tenant_id`。

列表接口的筛选参数按精确字符串匹配；仅对这些只读筛选参数，传入空字符串、未知状态或不存在的实体 ID 时没有匹配项，返回空数组，不报错。创建、更新、领取和完成等写接口中的未知枚举仍属于非法输入，必须拒绝。

### 事件发布与扇出

发布事件时，只读取当时处于 `ENABLED` 且事件类型匹配的订阅。`event_types` 包含精确的 `event_type` 或包含 `*` 即为匹配。每个匹配订阅生成一个 Delivery，创建顺序按 Subscription ID 数字后缀升序。

同一租户内，`idempotency_key` 标识一次事件发布意图。相同 key 与完全相同的 `event_type`、`payload`、`ordering_key` 重放时，直接返回首次创建响应快照，不新增 Event、Delivery 或审计；任一字段不同则为冲突，拒绝且不改变状态。不同租户使用独立命名空间。

Delivery 创建时复制 Endpoint 的 `lease_seconds`、`max_attempts`、`base_delay_seconds`、`max_delay_seconds` 和 `event_ttl_seconds` 到 `policy`，并令 `available_at = created_at`、`expires_at = created_at + event_ttl_seconds`。之后的端点策略更新不改写已有 Delivery。

禁用订阅只影响后续事件扇出；已经生成的 Delivery 继续流转。重新启用后仅匹配之后发布的事件。

### 领取、额度与顺序键

worker 只能从 `ACTIVE` 且可领取的 Endpoint 领取任务。Endpoint 为 `PAUSED` 或 `OPEN` 时不得发出新租约，但已存在的租约保持有效。

领取时先扣除该 Endpoint 当前 `LEASED` 数量，可用额度为当前 `max_inflight` 减去该数量。候选 Delivery 必须已到 `available_at`、未到 `expires_at`，并处于 `PENDING` 或 `RETRY_WAIT`。最终领取数不超过请求 `limit` 和可用额度。

候选任务按 `available_at`、`created_at`、Delivery ID 数字后缀依次排序。非空 `ordering_key` 在同一个 Endpoint 内严格串行：只要同 key 存在 Delivery ID 数字后缀更小且尚未进入 `SUCCEEDED` 或 `DEAD` 的 Delivery，后续任务就不能领取；回放任务按它新分配的 Delivery ID 参与比较。某个候选受阻时跳过它并继续检查排序靠后的候选。空 key 不启用此限制。

成功领取时，Delivery 转为 `LEASED`，`attempt_count += 1`，生成确定性的 `lease_token = "lease_<delivery_id>_<attempt_count>"`，并令 `lease_expires_at = now + policy.lease_seconds`。响应为 `{"deliveries": [...]}`，其中每项都是领取后的完整 Delivery，包含本轮 `lease_token` 和 `lease_expires_at`。GET 单项和列表查询同样返回完整 Delivery；LEASED 状态可见当前租约字段，其他状态这两个字段为 `null`。

### 完成、重试与迟到结果

只有当前 `LEASED` Delivery 且 `lease_token` 完全匹配时，首次完成回报才能生效。租约已过期、任务已终态、token 错误或旧轮次 token 的新完成意图都必须拒绝且不改变状态。

`completion_key` 在单个 Delivery 内提供幂等：首次有效完成后保存请求指纹和完整 Delivery 响应快照；相同 key、相同 token、相同 result 重放返回首次响应，不重复结算或审计；相同 key 携带不同字段时冲突拒绝。幂等记录检查先于当前租约状态检查，因此完全相同的已成功请求即使在任务终态或原租约到期后重放，仍返回首次响应。

- `SUCCESS`：Delivery 进入 `SUCCEEDED`，清空租约字段和失败结果。
- `PERMANENT`：Delivery 立即进入 `DEAD`，生成 reason=`PERMANENT` 的 DeadLetter。
- `RETRYABLE`：本次失败先计入 Endpoint 连续失败；若已达到 `max_attempts`，进入 `DEAD` 并生成 reason=`MAX_ATTEMPTS` 的 DeadLetter；否则进入 `RETRY_WAIT`。

第 `attempt_count` 次领取失败后的退避为：

```text
delay = min(base_delay_seconds * 2^(attempt_count - 1), max_delay_seconds)
available_at = 失败发生时刻 + delay
```

租约到期仍未完成，等价于在 `lease_expires_at` 时发生一次 `RETRYABLE`，并把 `last_result` 记为 `LEASE_TIMEOUT`。大跨度推进时仍以实际租约到期时刻计算下一次 `available_at`，不能以推进后的目标时刻代替。

### TTL 与死信

虚拟时钟到达 `expires_at` 时，尚未终态的 Delivery 进入 `DEAD`，`last_result = EXPIRED`，并生成 reason=`EXPIRED` 的 DeadLetter。TTL 与租约恰好同时到期时先执行 TTL 终止，不再安排重试。

一个 Delivery 最多生成一个 DeadLetter。进入 `SUCCEEDED` 或 `DEAD` 后不再接受领取或状态修改；两者都视为顺序键队列中的终态，不阻塞后续任务。

### 熔断与半开探测

端点当前策略中的 `failure_threshold` 和 `cooldown_seconds` 用于运行时熔断。一次 `SUCCESS` 会把 CLOSED 状态下的 `failure_streak` 清零；一次 `RETRYABLE`、`PERMANENT` 或租约超时会使其加一，即使该失败同时因达到最大次数而进入 DEAD。TTL 到期不计入连续失败。

CLOSED 状态下连续失败达到阈值后进入 `OPEN`，设置 `open_until = 失败发生时刻 + cooldown_seconds`。OPEN 不取消已有租约，只阻止新领取。

虚拟时钟到达 `open_until` 后进入 `HALF_OPEN`。HALF_OPEN 仍复用普通候选资格、排序、顺序键和 `max_inflight - 当前 LEASED 数量` 额度规则，但一次 claim 最多返回一个探测租约。Endpoint 在该探测完成或超时前始终只允许一个在途探测，即使虚拟时钟继续推进或再次调用 claim 也返回空数组；当次没有可领取候选时不会消耗探测机会。探测的身份以领取时记录为准，不因暂停、恢复或策略更新改变。探测成功后转回 `CLOSED`、清零失败次数；探测失败或超时后立即重新 OPEN，并从该失败时刻计算新的 `open_until`。

OPEN 不取消此前发出的普通租约。此类非探测租约在 OPEN 或 HALF_OPEN 期间成功完成时不关闭熔断器、也不清零失败次数；失败时仍增加 `failure_streak`，但不改变当前 OPEN 的 `open_until`，也不使 HALF_OPEN 重新 OPEN。端点进入 HALF_OPEN 时，既有租约仍计入 `max_inflight`，只有领取时登记的探测 Delivery 能控制探测成功或失败的状态转换。

端点暂停不改变熔断状态或在途租约；恢复后仍按当时熔断状态决定能否领取。

### 策略更新

策略更新对 Endpoint 当前字段原子生效。`max_inflight`、`failure_threshold` 和 `cooldown_seconds` 立即影响后续调度与熔断；已领取租约不被强制撤销。重试、租约时长和 TTL 仍使用各 Delivery 创建时保存的 `policy` 快照。

死信回放使用回放时 Endpoint 的最新 Delivery 策略快照创建一个新 Delivery，`created_at = available_at = 回放时 now`，`expires_at = 回放时 now + 最新 event_ttl_seconds`，`replay_of` 指向原 Delivery，其他租户、事件、订阅、端点和 ordering_key 保持不变。原 Delivery 与 DeadLetter 均保持终态和原字段，只在 DeadLetter 的 `replay_delivery_ids` 追加新 ID。

同一个 DeadLetter 内，`idempotency_key` 标识一次回放意图；key 以请求中的原始字符串精确比较，首尾空白只用于判定是否非空，不会被归一化，因此不同原始字符串是不同意图。完全相同的 key 重放返回首次创建响应快照，不重复创建 Delivery。

### 虚拟时钟

所有自动行为只由 `POST /clock/advance` 驱动。时钟只能向前推进正整数秒；失败请求不得改变时钟。推进响应只在目标时刻及此前所有到期动作结算完成后返回，因此不存在 `now` 已等于租约或 TTL 截止点、但该截止点尚未结算的可见状态；advance 返回后发起的完成请求按结算后的状态处理。

推进时需要处理跨越区间内的租约到期、TTL 和熔断冷却边界。实现应按实际到期时刻结算，保证一次推进很多秒与分段推进到相同终点得到相同业务结果。同一虚拟秒内固定先按 Delivery ID 处理全部 TTL 到期，再按 Delivery ID 处理仍为 LEASED 的租约超时，最后按 Endpoint ID 处理到期的 OPEN 到 HALF_OPEN 转换。

## Reset 约定

`POST /admin/reset` 是服务级操作，不需要业务参数。请求体可以省略，也可以传入任意 JSON 对象（推荐 `{}`）；格式错误或顶层不是对象的 JSON 请求应返回错误，不得改变状态。成功响应固定为 `{"status":"ok","now":0}`。

Reset 与其他业务请求串行执行。成功时一次性清理全部租户、端点、订阅、事件、投递和死信对象；重复调用仍成功且结果相同。若参数校验或执行失败，必须保持 reset 前的可观察状态不变；并发请求不能看到只清理了一部分的中间状态。
清理按下列清单顺序在同一互斥锁内完成，只有全部步骤成功后才返回成功响应。

成功 reset 的清理范围如下：

- **业务对象**：清空 `Tenant`、`Endpoint`、`Subscription`、`Event`、`Delivery` 和 `DeadLetter`；新建对象重新从各自的 `*_1` ID 开始。
- **关系、索引和派生视图**：同时移除 `Event.delivery_ids`、`DeadLetter.replay_delivery_ids` 及所有列表、筛选和详情视图中的关联记录，不留下悬空引用。
- **预留、锁和待处理任务**：清除 Delivery 的 `LEASED`、`RETRY_WAIT`、`PENDING` 状态、租约字段、重试安排以及 Endpoint 的半开探测标记；并发互斥锁本身保留，用于保证操作串行。
- **幂等与去重记录**：清空事件发布、完成回报和死信回放使用的幂等记录；旧 key 在 reset 后可以作为新意图再次使用。
- **事件、审计和历史**：清空事件与死信历史以及每个租户的审计序列；reset 不保留旧 `AuditEntry`。
- **计数器与逻辑时钟**：将租户、端点、订阅、事件、投递、死信的 ID 计数器全部归零，并将虚拟时钟设为 `now = 0`。
- **配置与参考数据**：保留服务代码和默认 Endpoint 策略等全局参考配置；reset 后新建 Endpoint 仍使用题面规定的默认值。
- **作用域外数据**：不访问或修改真实 Webhook、外部网络、进程启动配置和持久化数据库；本题没有需要 reset 的作用域外业务数据。

Reset 返回后可观察到 `/clock` 为 `{"now":0}`、各业务列表为空；随后创建的第一条同类对象使用 `*_1`，新租户的 `/audit` 从空数组开始。

## 本地自测命令提示

```bash
# 启动你的服务
PORT=8000 <你的启动命令，例如 python3 main.py> &
# 运行公开自测
python3 sample_testcases/run_sample_testcases.py
```

公开 sample 只验证服务连通性和少量基础契约；即使全部通过，也不代表任何最终用例或最终得分。

## 服务启动要求

- 服务必须读取 `PORT` 环境变量并绑定 `0.0.0.0`；本地未设置 `PORT` 时可使用 `8000` 作为默认值，判题环境可能注入其他可用端口，不能把线上端口写死为 8000。
- 服务必须以前台常驻进程运行，所有接口收发 JSON。
- 入口必须位于工作区根目录。
- 服务需要支持连续请求；共享内存状态必须保证并发访问安全。

| 语言          | 入口                                  | 启动命令                                                     |
| ------------- | ------------------------------------- | ------------------------------------------------------------ |
| Python        | `main.py`                             | `python3 main.py`                                            |
| Go            | `main.go` + `go.mod`                  | `go build . && ./<binary>`                                   |
| Java（Maven） | `pom.xml` + `src/main/java/Main.java` | `mvn package -DskipTests && java -jar target/webhook-delivery-system-1.0.0.jar` |
| JavaScript    | `main.js`                             | `node main.js`                                               |
| TypeScript    | `main.ts` + `tsconfig.json`           | `tsx main.ts`                                                |
| C++           | `main.cpp`                            | `g++ -std=c++17 -O2 -pthread main.cpp -o main && ./main`     |

## 开发环境说明

作答环境不允许访问普通外网，但允许使用常规依赖管理命令安装第三方依赖，例如 `mvn`、`go get`、`npm install` 或 `pip install`。下表列出镜像中已经预装、可直接使用的依赖与版本，但预装清单不是可用依赖白名单。新增依赖应写入对应项目的依赖清单或锁定文件，确保判题环境能从干净工作区重现安装与构建；不要依赖仅存在于当前终端的全局安装。

| 语言          | 版本                    | 可直接使用的能力                                           |
| ------------- | ----------------------- | ---------------------------------------------------------- |
| Python        | 3.11                    | 标准库、FastAPI、uvicorn、Flask、requests、httpx、pydantic |
| Go            | 1.22                    | 标准库                                                     |
| Java（Maven） | JDK 21 + Maven          | Maven 工程、标准库、`com.sun.net.httpserver`               |
| JavaScript    | Node.js 22              | Node.js 标准库、Express 4.21.2                             |
| TypeScript    | Node.js 22 + tsx 4.19.2 | Node.js 标准库、Express 4.21.2                             |
| C++           | g++ 12，C++17           | 标准库；镜像提供 nlohmann-json 3.10.5                      |



# Webhook 事件投递与重试编排中枢 - 技术方案

## 1. 架构概述
本系统是一个基于 Node.js 的常驻 HTTP 服务，采用**内存状态管理**和**虚拟时钟驱动**的设计模式。核心目标是实现确定性的 Webhook 投递编排，包括租户隔离、任务扇出、领取租约、结果回报、退避重试、熔断机制及死信回放。

### 1.1 模块划分
- **`main.js`**: 服务入口，负责启动 HTTP 服务器并绑定端口。
- **`webhook/http.js`**: 基础工具层，处理 JSON 序列化、请求体读取和响应发送。
- **`webhook/state.js`**: **核心状态管理层**。维护所有业务实体（Tenant, Endpoint, Subscription, Event, Delivery, DeadLetter, AuditEntry）的内存存储、ID 计数器、虚拟时钟以及幂等性记录。
- **`webhook/routes.js`**: **路由与业务逻辑层**。解析 URL 和参数，调用 `state` 模块执行业务操作，并处理参数校验和错误返回。

## 2. 数据结构设计 (`webhook/state.js`)

### 2.1 全局状态容器
使用 `Map` 或对象数组存储实体，确保查询效率：
- `tenants`: `Map<tenant_id, Tenant>`
- `endpoints`: `Map<endpoint_id, Endpoint>`
- `subscriptions`: `Map<subscription_id, Subscription>`
- `events`: `Map<event_id, Event>`
- `deliveries`: `Map<delivery_id, Delivery>`
- `deadLetters`: `Map<dead_id, DeadLetter>`
- `auditLogs`: `Map<tenant_id, AuditEntry[]>`
- `idempotencyCache`: `Map<string, any>` (用于存储首次成功的响应快照)

### 2.2 ID 生成器
为每种实体维护独立的递增计数器：
- `nextTenantId`, `nextEndpointId`, `nextSubscriptionId`, `nextEventId`, `nextDeliveryId`, `nextDeadLetterId`
- Reset 时全部归零。

### 2.3 虚拟时钟
- `now`: 全局整数秒，初始为 0。
- 仅通过 `/clock/advance` 接口推进。

### 2.4 关键实体扩展字段
- **Endpoint**: 增加 `probe_delivery_id` (string | null)，用于标记 `HALF_OPEN` 状态下的独占探测任务。
- **Delivery**: 
  - `policy`: 创建时锁定的策略快照对象。
  - `lease_token`: 格式为 `"lease_<delivery_id>_<attempt_count>"`。
- **AuditEntry**: 包含 `seq`, `at`, `type`, `entity_id`。

## 3. 核心业务流程实现

### 3.1 事件发布与扇出 (`POST /events`)
1. **幂等检查**: 检查 `(tenant_id, idempotency_key)` 是否已存在缓存。若存在且内容一致，返回缓存快照；若内容冲突，返回 409。
2. **匹配订阅**: 查找该租户下 `status=ENABLED` 且 `event_types` 匹配的订阅。
3. **生成 Delivery**: 
   - 按 Subscription ID 升序遍历。
   - 复制 Endpoint 当前策略到 `policy`。
   - 设置 `available_at = now`, `expires_at = now + event_ttl_seconds`。
4. **审计**: 追加 `EVENT_PUBLISHED` 和多个 `DELIVERY_CREATED`。
5. **缓存**: 保存完整响应快照。

### 3.2 任务领取 (`POST /dispatch/claim`)
1. **前置校验**: Endpoint 必须存在、`ACTIVE` 且非 `OPEN`。
2. **额度计算**: `available = max_inflight - count(LEASED deliveries for this endpoint)`。
3. **候选筛选**: 
   - 状态为 `PENDING` 或 `RETRY_WAIT`。
   - `available_at <= now` 且 `expires_at > now`。
4. **排序**: `available_at` -> `created_at` -> Delivery ID 数字后缀。
5. **顺序键阻塞**: 遍历时，若候选有 `ordering_key`，检查是否存在同 key 且 ID 更小、状态非终态的 Delivery。若有则跳过。
6. **熔断探测**: 
   - 若 Endpoint 为 `HALF_OPEN` 且已有 `probe_delivery_id`，直接返回空数组。
   - 若为 `HALF_OPEN`，最多领取 1 个任务作为探测，并记录 `probe_delivery_id`。
7. **状态更新**: 
   - Delivery 转为 `LEASED`。
   - `attempt_count++`。
   - 生成 `lease_token` 和 `lease_expires_at`。
8. **审计**: 追加 `DELIVERY_CLAIMED`。

### 3.3 结果回报 (`POST /deliveries/{id}/complete`)
1. **幂等检查**: 检查 `(delivery_id, completion_key)`。若已存在且字段一致，返回缓存快照。
2. **租约校验**: Delivery 必须为 `LEASED` 且 `lease_token` 匹配，且 `lease_expires_at > now`。
3. **状态流转**:
   - **SUCCESS**: 转 `SUCCEEDED`，清零 Endpoint 失败计数（若为 CLOSED）。
   - **PERMANENT**: 转 `DEAD`，生成 DeadLetter (`reason=PERMANENT`)。
   - **RETRYABLE**: 
     - 增加 Endpoint 失败计数。
     - 若达 `max_attempts`，转 `DEAD`，生成 DeadLetter (`reason=MAX_ATTEMPTS`)。
     - 否则转 `RETRY_WAIT`，计算 `available_at = now + delay`。
4. **熔断更新**: 
   - 成功则清零 `failure_streak`。
   - 失败则 `failure_streak++`，若达阈值则进入 `OPEN`。
   - 探测任务成功则转 `CLOSED`，失败则重新 `OPEN`。
5. **审计**: 追加相应类型日志。

### 3.4 虚拟时钟推进 (`POST /clock/advance`)
1. **目标时刻**: `target = now + seconds`。
2. **批量结算** (按实际发生时刻排序执行):
   - **TTL 到期**: 找出 `expires_at` 在 `(now, target]` 之间且未终态的 Delivery，转 `DEAD` (`EXPIRED`)，生成 DeadLetter。审计 `at` 为 `expires_at`。
   - **租约超时**: 找出 `lease_expires_at` 在 `(now, target]` 之间且仍为 `LEASED` 的 Delivery，视为 `RETRYABLE` (`LEASE_TIMEOUT`)，触发重试逻辑。审计 `at` 为 `lease_expires_at`。
   - **熔断转换**: 找出 `open_until` 在 `(now, target]` 之间的 Endpoint，转 `HALF_OPEN`，清空 `probe_delivery_id`。审计 `at` 为 `open_until`。
3. **更新时钟**: `now = target`。

### 3.5 死信回放 (`POST /dead_letters/{id}/replay`)
1. **幂等检查**: 检查 `(dead_id, idempotency_key)`。
2. **创建新 Delivery**: 
   - 复制原 Delivery 的大部分字段。
   - `policy` 使用**当前** Endpoint 的策略。
   - `created_at = available_at = now`。
   - `replay_of` 指向原 Delivery ID。
3. **更新 DeadLetter**: 在 `replay_delivery_ids` 中追加新 ID。
4. **审计**: 追加 `DEAD_LETTER_REPLAYED` 和 `DELIVERY_CREATED`。

## 4. 接口实现清单 (`webhook/routes.js`)

| 方法 | 路径 | 状态 | 备注 |
|---|---|---|---|
| GET | `/health` | ✅ 已实现 | |
| POST | `/admin/reset` | ✅ 已实现 | 需完善 state.reset() |
| GET | `/clock` | ✅ 已实现 | |
| POST | `/clock/advance` | ⬜ 待实现 | 核心逻辑 |
| POST | `/tenants` | ✅ 已实现 | |
| GET | `/tenants` | ✅ 已实现 | |
| GET | `/tenants/{id}` | ✅ 已实现 | |
| POST | `/endpoints` | ⬜ 待实现 | |
| GET | `/endpoints` | ⬜ 待实现 | 支持 tenant_id 筛选 |
| GET | `/endpoints/{id}` | ⬜ 待实现 | |
| POST | `/endpoints/{id}/pause` | ⬜ 待实现 | |
| POST | `/endpoints/{id}/resume` | ⬜ 待实现 | |
| POST | `/endpoints/{id}/policy` | ⬜ 待实现 | |
| POST | `/subscriptions` | ⬜ 待实现 | |
| GET | `/subscriptions` | ⬜ 待实现 | 支持 tenant_id, endpoint_id 筛选 |
| GET | `/subscriptions/{id}` | ⬜ 待实现 | |
| POST | `/subscriptions/{id}/disable` | ⬜ 待实现 | |
| POST | `/subscriptions/{id}/enable` | ⬜ 待实现 | |
| POST | `/events` | ⬜ 待实现 | 核心逻辑 |
| GET | `/events` | ⬜ 待实现 | 支持 tenant_id 筛选 |
| GET | `/events/{id}` | ⬜ 待实现 | |
| POST | `/dispatch/claim` | ⬜ 待实现 | 核心逻辑 |
| POST | `/deliveries/{id}/complete` | ⬜ 待实现 | 核心逻辑 |
| GET | `/deliveries` | ⬜ 待实现 | 支持多条件筛选 |
| GET | `/deliveries/{id}` | ⬜ 待实现 | |
| GET | `/dead_letters` | ⬜ 待实现 | 支持 tenant_id, endpoint_id 筛选 |
| GET | `/dead_letters/{id}` | ⬜ 待实现 | |
| POST | `/dead_letters/{id}/replay` | ⬜ 待实现 | |
| GET | `/audit` | ⬜ 待实现 | 必须提供 tenant_id |

## 5. 实施步骤建议

1. **完善 `state.js`**: 
   - 定义所有实体的数据结构。
   - 实现 ID 生成器和虚拟时钟管理。
   - 实现基础的 CRUD 方法。
   - 实现 `reset()` 清理所有状态。
2. **实现配置管理接口**: Tenant, Endpoint, Subscription 的增删改查。
3. **实现事件发布逻辑**: 包含订阅匹配和 Delivery 生成。
4. **实现任务领取逻辑**: 包含额度、排序、顺序键和熔断探测。
5. **实现结果回报逻辑**: 包含状态流转、退避计算和熔断更新。
6. **实现虚拟时钟推进**: 实现 TTL、租约超时和熔断转换的批量结算。
7. **实现死信与回放**。
8. **实现审计日志**。
9. **完善 `routes.js`**: 将所有业务逻辑对接到 HTTP 路由，并添加参数校验。
10. **测试与调试**: 使用 `sample_testcases/run_sample_testcases.py` 进行连通性测试。

## 6. 注意事项
- **幂等性**: 所有写操作需考虑幂等键冲突。
- **原子性**: `reset` 和 `advance` 等操作需保证状态一致性。
- **性能**: 虽然数据量不大，但 `advance` 时的批量结算应避免不必要的循环。
- **规范**: 严格遵守题目要求的字段名、枚举值和返回格式。