ai互动图文的ai幻觉怎么办

有世界观，主线，角色介绍，参考图

怎么测试

逻辑单元测试，AI用agent跑自动化脚本

AI对话有哪些功能

语音输入，websocket+see队列降级

markdown

stt,tts实现

分贝检测，串行化语音示例

贪心分片，串行请求，用并行的方式考虑顺序

funcall

和mcp有什么区别

aicr一套，skill优化，分桶策略

mcp和skill的区别

sse和websocket有什么区别，sse和轮询的区别

文本sse，图片轮询请求

闭包是什么，存在的意义是什么

内存溢出和内存泄露的含义

函数调用栈和作用域

- **调用栈** 记录 `a` 调用了 `b`
- **作用域链** 让 `b` 能访问 `a` 里的 `x`（闭包的底层机制）

事件循环

```js
const p1 = new Promise((resolve) => {
    console.log(0)
    resolve()
}).then(function f1() {
    console.log(1)
    const p2 = new Promise(resolve => {
      console.log(2)
      resolve()
    }).then(function f3() {
      console.log(3)
    }).then(function f4() {
      console.log(4)
    })
}).then(function f2() {
    console.log(5)
})
console.log(6)
//0612354   先f3再f2再f4
```

mini-vue响应式