#### vue题

##### 一个按钮点击变成loading加载状态 并且不能被点击

#### ref,reactive区别

- 返回值类型
  ref → `Ref<T>`（对象，需 `.value` 读写）；
  reactive → `Proxy` 对象，可直接读写属性。
- 基本类型
  基本类型（number、string…）只能用 ref；reactive 只能包对象。
- 解构/展开
  reactive 对象解构会丢失响应式；ref 解构后仍是 Ref，可继续用 `.value`

#### vue的指令

v-if / v-else-if / v-else
v-show
v-for
v-bind（缩写 :）
v-on（缩写 @）
v-model
v-slot
v-text
v-html
v-cloak
v-once
v-memo

#### 自定义指令实现

```js
const vFocus = {
  mounted(el) { el.focus() }
}
```

#### 为什么templete不需要加value

编译阶段，Vue 的模板编译器会把模板中的表达式做「自动解包」：

- 如果解析到的是一个 Ref，自动在生成的渲染函数里加 `.value`；
- 所以开发者手写时可以省。
  在 `<script setup>` 或普通 JS 里仍需手动 `.value`

#### 0.1+0.2 =0.3？

js数据用二进制双精度浮点 十进制转换为无限循环小数 导致误差



#### 鉴权 token

- 登录后后端颁发：
  `{ header, payload, signature }` 三段 base64 字符串。

- 前端存：localStorage / sessionStorage / Cookie（需设 HttpOnly、Secure）。

- 请求携带：

  ```
  Authorization: Bearer <token>
  ```

#### token过期

- 后端：JWT payload 中 `exp` 字段；API 返回 401。
- 前端：
  1. 响应拦截器捕获 401 → 清除本地 token → 跳转登录；
  2. 或结合「刷新 token」：
     同时返回 `accessToken`(短) + `refreshToken`(长)。
     当 accessToken 过期，用 refreshToken/refresh 调  换一对新令牌。

#### HTTP头有哪些 

1. 通用首部

- Cache-Control
- Connection
- Date
- Via

请求首部

- Accept / Accept-Encoding / Accept-Language
- Authorization
- Cookie
- Host
- Referer
- User-Agent

响应首部

- Server
- Set-Cookie
- WWW-Authenticate

实体首部

- Content-Type
- Content-Length
- Content-Encoding
- Last-Modified
- ETag

安全/跨域

- Access-Control-Allow-Origin
- Access-Control-Allow-Methods
- X-Content-Type-Options
- Strict-Transport-Security