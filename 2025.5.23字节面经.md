### 项目介绍

业务功能

用户权限菜单路由

组件传配置

路由权限动态生成

**组件库的按需引入--原理具体做了什么事情**

**localstroge,sessionstroge,indexDB**

- **`localStorage`**：简单的键值存储，数据持久化存储。
- **`sessionStorage`**：简单的键值存储，数据仅在当前会话期间有效。
- **`IndexedDB`**：复杂的结构化存储，支持大量数据和复杂查询。异步

**prettierrc,eslint配置**

### vue3特点，优点缺点

更快的渲染速度，响应式原理，tree-shaking

##### 浏览器渲染原理

##### 跨域原因，如何解决

jsonp,代理，后端cors，允许请求源，WebSocket 

##### 事件循环机制

## css

position属性

**绝对定位偏移量根据什么**

相对于其最近的定位祖先元素来计算的。如果没有定位祖先元素，则相对于初始包含块（通常是 `<html>` 元素）来计算。通过设置 `top`、`right`、`bottom` 和 `left` 属性，可以精确控制绝对定位元素的位置

**垂直居中方式**

flex,绝对定位，行高等于高度，align-content，grid

**margin重叠**

相邻兄弟元素的垂直外边距重叠

父子元素的垂直外边距重叠

##### promise控制并发

```js
//all包循环
async function a(urls, maxNum) {
  const results = [];
  let index = 0;
  async function worker() {
    while (index < urls.length) {
      const i = index++; // 原子取索引
      const res = await fetch(urls[i]).then((res) => res.text())
      results.push(res);
      console.log(results);
    }
  }
  // 启动 maxNum 个 worker，全部完成后返回
  const workers = Array(Math.min(maxNum, urls.length))
    .fill(null)
    .map(() => worker());
  // await Promise.all(workers);
  // return results;同样的效果
  return Promise.all(workers).then(() => {
    return results;
  });
}
```

```js
//循环包all
async function c(urls, maxNum) {
  let results = [];
  for (let i = 0; i < urls.length; i += maxNum) {
    let arr = urls.slice(i, i + maxNum);
    await Promise.all(
      arr.map((url) => fetch(url).then((res) => res.text())),
    ).then((res) => {
      results.push(...res);
    });
  }
  return results;
}
```

##### 格式化数字

1000->1,000

```js
function a(num){
  let arr = []
  let str = num.toString().split('').reverse()
  for(let i = 0; i < str.length; i++){
    if(i!==0 && i % 3 === 0){
      arr.push(',')
    }
    arr.push(str[i])
  }
  return arr.reverse().join('')
}
```

##### vscode插件开发，webview