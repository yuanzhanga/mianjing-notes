### webpack,vite的区别，vite快的原因

**Vite开发阶段利用浏览器原生 ESM 按需编译跳过打包，生产阶段用 Rollup 深度优化**

**Webpack** 是传统的“打包优先”模式。它需要从入口文件开始，递归解析所有依赖，构建完整的依赖图（Dependency Graph），然后使用 Loader 逐个处理模块，最终将所有模块打包成一个或多个 Bundle 文件，才能启动开发服务器。

**Vite** 则基于原生 ES 模块（ESM），采用“按需编译”模式。它在开发环境下不进行打包，而是直接启动一个开发服务器，当浏览器请求某个模块时，Vite 才实时编译并返回该模块。

**Webpack 的 HMR**：文件修改后，需重新构建变更模块及其依赖链，通过 WebSocket 推送更新，速度随项目规模增大而下降。**重新编译依赖链 → 刷新页面/局部更新**

**Vite 的 HMR**：基于原生 ESM，仅编译变更的单个文件，通过 WebSocket 精准推送，更新速度稳定在毫秒级，不受项目规模影响。 模块级替换，**只推送变更模块**

**Webpack**：使用自身内核打包，拥有更成熟的代码分割、Tree-shaking 优化

**Vite**：使用 Rollup 进行打包，得益于 Rollup 优秀的 Tree-shaking 能力，通常能生成更小的打包体积

### webpack

##### 常见的优化手段

**构建速度优化**

1. 缓存
   - **`cache` 配置**：Webpack 5 内置持久化缓存，开启后二次构建速度大幅提升
   - **Babel 缓存**：`babel-loader` 开启 `cacheDirectory: true`。
   - **ESLint 缓存**：`eslint-webpack-plugin` 开启 `cache: true`
2. 并行处理
   - **`thread-loader`**：将耗时的 loader（如 babel-loader、ts-loader）放在 worker 池中并行执行
   - **`parallel-webpack`**：多进程并行构建多个配置。
3. 使用更快的工具替代
   - **`esbuild-loader`** / **`swc-loader`**：替代 babel-loader，编译速度提升 10-20 倍。
   - **`terser-webpack-plugin`** 配合 `esbuild` 或 `swc` 进行代码压缩。
   - **`rspack`**：基于 Rust 的 Webpack 替代品，构建速度极快。
4. 按需构建，Inquirer.js

**产物体积优化**

- 代码分割，动态导入，路由/组件懒加载
- 预加载，`<link rel="preload/prefetch">` 配合 `webpackPrefetch` / `webpackPreload` 魔法注释
- 移除无用代码，
  - Tree Shaking，**Tree-shaking** 是现代构建工具（如 Webpack、Rollup）的一种优化机制，用于移除未使用的代码。它基于 ESModule 的静态分析，能够识别出哪些模块被实际使用，从而在打包时移除未使用的代码
  - **`purgecss-webpack-plugin`**插件移除未使用的 CSS
- 按需加载，`unplugin-vue-components` 用于 Vue 中实现组件的自动按需引入
  1. **解析模板代码**：在构建过程中，`unplugin-vue-components` 会解析 Vue 模板代码，查找模板中使用的组件标签。
  2. **匹配组件名称**：通过正则表达式匹配模板中的组件标签（如 `<a-button>`），并将其标准化为组件名称（如 `AButton`）。
  3. **使用解析器**：将标准化后的组件名称传递给配置的解析器（如 **ElementPlusResolver**），解析器会返回组件的导入路径和样式路径。
  4. **生成导入语句**：根据解析器返回的信息，生成对应的导入语句，并将其插入到代码中。
  5. **替换模板代码**：将模板中的 `_resolveComponent` 替换为具体的组件引用，完成按需引入

**运行时性能优化**

1. 模块联邦，实现微前端架构，独立部署、共享依赖
2. 长期缓存

| 工具                           | 用途                      |
| ------------------------------ | ------------------------- |
| `webpack-bundle-analyzer`      | 可视化分析产物体积        |
| `speed-measure-webpack-plugin` | 分析各 loader/plugin 耗时 |
| `webpackbar`                   | 显示构建进度              |

##### Rspack

| 度           | Webpack    | Vite                                          | Rspack                           |
| ------------ | ---------- | --------------------------------------------- | -------------------------------- |
| **语言**     | JavaScript | JavaScript + esbuild(Rust)                    | **Rust**                         |
| **架构**     | 打包器     | 开发时 ESM + 生产 Rollup（rust重写 Rolldown） | **打包器**                       |
| **兼容性**   | 生态最成熟 | 新生态                                        | **Webpack 插件/loader 兼容**     |
| **速度**     | 慢         | 开发极快                                      | **构建极快（10~20 倍 Webpack）** |
| **适用场景** | 遗留项目   | 新项目                                        | **大型遗留项目无痛迁移**         |

##### es6代码如何转化为es5

 **babel-loader**--怎么实现？

解析-->转换-->生成

Babel 转译 ES6 到 ES5 时，**词法分析阶段**将源码字符串按规则切分为 Token 流（识别关键字、标识符、运算符等最小语义单元），**语法分析阶段**再按 JS 语法规则将 Token 流组装成 AST 树（检查语法合法性并构建代码的层级结构），随后进入转换阶段由插件遍历 AST 节点进行替换，Babel 的**转换阶段**采用 Visitor 模式深度遍历 AST，当匹配到特定节点类型（如 `ArrowFunctionExpression`）时触发对应插件的处理函数，将 ES6 节点替换为等价的 ES5 节点（如箭头函数转为普通函数并处理 `this` 绑定），最后**生成阶段**递归遍历修改后的 AST，按规则拼接字符串并输出 ES5 代码及 Source Map

### Vite

Vite 跳过打包 一些**潜在问题**:

- 浏览器兼容性限制

- 请求 1 个入口 → 发现 50 个 import → 并发请求 50 个模块,大量模块导致请求瀑布流(HTTP/2 缓解：多路复用降低开销)
- 开发esbuild 预构建，把第三方依赖"翻译"成ESM格式，生产用rollup,生产构建与开发行为不一致----实习时开发完没问题但是到test环境出现bug，组件库版本不一样，将组件提取成预编译组件,jsx----统一 lock 文件，CI 构建前检查版本

```js
export default defineConfig({
  plugins: [
    uni()
  ],
    //把5172端口代理到3000端口，防止后端跨域
  server: {
    port: 5172,
    proxy: {
      '/api': {
        target: 'http://localhost:3000',
        changeOrigin: true
      }
    }
  }
})

```

**构建**

`assets` 里的文件会被构建工具压缩、加 hash、转格式、内联等优化，让网站加载更快、缓存更智能；`public` 里的文件原样复制，不做任何处理

**vite8新特性**

- 模块级持久缓存，第二次构建可提速 80%。

- **Rolldown**统一开发生产工具，开发/生产**行为一致**，不再有两套打包逻辑的差异

  | 阶段          | Vite 7                  | Vite 8                       |
  | ------------- | ----------------------- | ---------------------------- |
  | 开发（dev）   | esbuild 转译 + 原生 ESM | **Rolldown** 转译 + 原生 ESM |
  | 生产（build） | Rollup 打包             | **Rolldown** 打包            |

### cjs,esm

**CJS 是运行时同步加载、值拷贝，适合服务端；ESM 是编译时静态分析、动态引用，支持 Tree Shaking，是浏览器和现代 Node 的标准。核心差异在加载时机和导出机制**

CommonJS模块规范使用require语句导入模块，module.exports导出模块，require命令第一次加载该脚本时就会执行整个脚本，以后用到这个模块时，就会到对象的exports属性中取值。即使再次执行require命令，也**不会再次执行该模块，而是到缓存中取值**。输出的是**值的拷贝**，模块导入的也是输出值的拷贝，也就是说，一旦输出这个值，这个值在模块内部的变化是监听不到的。



ES6模块的规范是使用import语句导入模块，export语句导出模块，输出的是对**值的引用**。遇到模块加载命令import时只会生成一个动态的只读引用，等真的需要用到这个值时，再到模块中取值，原始值变了那输入值也会发生变化。

阶段一：构建（Construction），根据地址查找js文件，并且下载，将其**解析成模块记录**（Module Record）缓存到module map中；

阶段二：实例化（Instantiation），对模块记录进行实例化，并且**为变量分配内存空间**，解析模块的导入和导出语句，把模块指向 对应的内存地址。从依赖关系的**最后一个模块**（没有引入其他模块）开始实例化，并将所有模块导出的变量绑定在内存上，然后再将所有模块导入变量绑定到与导出变量同一个内存区域。所以一旦导出值发生变化，导入值也会变化。这也是ESM导出的是值引用的原理

阶段三：运行（Evaluation），运行代码，计算值，并且将值填充到内存地址中



循环引用

1）CommonJS模块是加载时执行。一旦出现某个模块被“循环加载”，就只输出已经执行的部分，没有执行的部分不会输出。

2）ES6模块对导出模块，变量，对象是动态引用，遇到模块加载命令import时不会去执行模块，只是生成一个指向被加载模块的引用。

CommonJS模块规范主要适用于后端Node.js，后端Node.js是同步模块加载，所以在模块循环引入时模块已经执行完毕。推荐前端工程中使用ES6的模块规范，通过安装Babel转码插件支持ES6模块引入的语法。

### Tree shaking

将文件中的全局语句定义为 part ，并将 part 标记为是否有副作用。比如语句 let foo = 3 就没有副作用，如果没有用到 foo 就可以将这个语句删除。但是 let foo = bar() 就有副作用，即使没有用到 foo，无法保证移除函数调用 bar() 之后程序行为一致。
Tree shaking 通过从入口文件（这里是index.js）访问所有有副作用的 part，并沿着 import语句访问所有引用文件中有副作用的 part，只有这个过程中访问到的 part 才会被放进打包结果中

### ESLint，Prettier

ESLint检查代码中的**潜在错误**和**不规范的写法**，强制执行代码风格规则

- 使用了未定义的变量、拼写错误
- 禁止使用 `var`、要求组件命名大写
- `==` 代替 `===`、异步操作未处理错误

Prettier统一代码的**外观格式**，不关心逻辑对错

- 单引号还是双引号
- 行尾是否加分号
- 箭头函数参数是否加括号

### 缓存

Cookie 传身份，LocalStorage 存配置，SessionStorage 存临时态，IndexedDB 存大数据，Cache Storage做离线，OPFS 搞高性能文件

####   OPFS

浏览器内部文件系统，提供接近原生文件系统的性能，高性能文件读写、Wasm

```
env.cacheDir = 'opfs://models' //用 OPFS 缓存模型权重
const root = await navigator.storage.getDirectory();
const newFile = await root.getFileHandle(fileName, { create: true });
const writable = await newFile.createWritable();
await writable.write(blob);
await writable.close();
newFile.getFile();
```

### MutationObserver

监听DOM属性变化,标签上的属性

```js
const docObserver = new MutationObserver((mutations, observer) => {
    mutations.forEach(mutation => {});
});
docObserver.observe(document.documentElement, options);
```

### IntersectionObserver

**元素可见性监听 API**，异步检测元素是否进入视口，用于懒加载、无限滚动、动画触发，广告曝光统计，多阈值监听

```
const observer = new IntersectionObserver((entries, observer) => {
   entries.forEach((entry) => {
     // entry.target: 目标元素
     // entry.isIntersecting: 是否进入视口
     // entry.intersectionRatio: 交叉比例 (0 ~ 1)，阈值
     // entry.boundingClientRect: 元素位置信息
     // entry.intersectionRect: 交叉区域位置
     // entry.rootBounds: 根元素位置
     if (entry.isIntersecting) {
        console.log("元素进入视口:", entry.target);
     } else {
        console.log("元素离开视口:", entry.target);
     }
   });
},{
        root: null, // 根元素（null = 视口）
        rootMargin: "0px", // 根元素外边距
        threshold: 1, // 触发阈值 (0 ~ 1)，可数组
   },
);
const target = document.querySelector("#myInput");
observer.observe(target);
```

### Worker

Worker的消息传递都是深拷贝，不改变原始数据

**WebWorker** 

**SharedWorker** 只要指向文件是同一个就共享的worker（单例）

**ServiceWorker** 通过页面注册ServiceWorker，一旦注册则成为页面对应站点(scope)的常驻后台程序

生命周期：install(一次)->activated(多次激活)->fetch拦截/message主线程通信->terminated(关闭)

```javascript
fetch拦截适用于静态数据，直接拦截从缓存里()拿数据
网络超时，网络慢或离线 → 自动回退缓存，用缓存兜底，用户无感知
```

### 微前端

把**一个庞大的前端应用拆分成多个独立部署、独立运行的小应用**，每个小应用可以由不同团队用不同技术栈开发和维护

通过运行时 JS 加载子应用

```
1. 路由匹配到 /react
        ↓
2. fetch 请求 http://localhost:3001
        ↓
3. 解析返回的 HTML，提取：
   - JS 入口文件（<script src="...">）
   - CSS 文件（<link href="...">）
        ↓
4. 用 eval / new Function / script 标签执行 JS
   （关键：需要隔离 window、document、路由）
        ↓
5. 子应用 mount 函数被调用，渲染到 #container
        ↓
6. 路由切换离开时，调用子应用 unmount 函数清理
```

| 框架                  | 实现方式                       | 特点                           | 适用场景               |
| --------------------- | ------------------------------ | ------------------------------ | ---------------------- |
| **qiankun**           | JS 运行时 + Proxy 沙箱         | 阿里出品，生态成熟，文档全     | 中后台系统，多技术栈   |
| **single-spa**        | 路由匹配 + 生命周期            | 最老牌，只负责调度，不处理加载 | 需要自研加载逻辑       |
| **Micro App**         | 类 Web Components              | 京东出品，基于 Custom Element  | 简单接入，零依赖       |
| **Module Federation** | 编译时模块共享                 | Webpack 官方，共享依赖最优     | 统一技术栈，组件级共享 |
| **wujie**             | Web Components + iframe 双引擎 | 腾讯出品，极致隔离             | 对隔离性要求极高       |

**微前端把大前端拆成小应用独立开发部署，底层通过 JS 沙箱隔离 window/document、CSS 隔离避免样式冲突、路由劫持实现切换，主流方案有 qiankun（运行时集成）和 Module Federation（编译时共享）两种路线。**

