# **计网**

## 应用层协议

| 关联协议                   | 作用                                                         |
| :------------------------- | :----------------------------------------------------------- |
| **TCP**                    | 传输层承载，HTTP/1.1 和 HTTP/2 基于 TCP                      |
| **TLS/SSL**                | HTTPS 的加密层，位于 HTTP 和 TCP 之间                        |
| **HTTP/2**                 | 基于 TCP + TLS，引入二进制分帧、多路复用、头部压缩（HPACK）、服务器推送 |
| **HTTP/3**                 | 基于 **QUIC**（UDP 之上），解决 TCP 队头阻塞                 |
| **DNS**                    | 域名解析，HTTP 请求前的第一步，默认查询使用 UDP 53 端口（快，小包） |
| **WebSocket**              | 握手完成后升级为 TCP 长连接                                  |
| **FTP**,TCP 20/21          | 明文传输，控制连接（21）+ 数据连接（20），被动/主动模式      |
| **FTPS**(TCP + TLS/SSL)    | FTP over TLS，显式（AUTH TLS）或隐式                         |
| **SFTP**,SSH (TCP 22)      | SSH 文件传输协议，与 FTP 完全不同                            |
| SMTP / POP3 / IMAP（邮件） |                                                              |
| **SPF/DKIM/DMARC**         | 邮件防伪验证机制                                             |

## 传输层协议

### TCP

| 关联机制        | 说明                             |
| :-------------- | :------------------------------- |
| **三次握手**    | SYN → SYN-ACK → ACK，建立连接    |
| **四次挥手**    | FIN → ACK → FIN → ACK，关闭连接  |
| **滑动窗口**    | 流量控制                         |
| **拥塞控制**    | 慢启动、拥塞避免、快重传、快恢复 |
| **Nagle 算法**  | 小数据包合并，减少网络负载       |
| **Delayed ACK** | 延迟确认，与 Nagle 可能冲突      |

### UDP

| 关联协议     | 用途           |
| :----------- | :------------- |
| **DNS**      | 域名查询       |
| **QUIC**     | HTTP/3 基础    |
| **RTP/RTCP** | 实时音视频传输 |
| **DHCP**     | 动态 IP 分配   |
| **SNMP**     | 网络管理       |

**特点**：无连接、不可靠、低延迟、支持广播/多播

## 网络层协议

### IP

| 版本     | 特点                                                  |
| :------- | :---------------------------------------------------- |
| **IPv4** | 32 位地址，NAT 广泛使用，TTL 字段                     |
| **IPv6** | 128 位地址，无 NAT 需求，流标签（Flow Label）支持 QoS |

## TCP三次握手

客户端给服务端发送新连接(SYN)和随机序列号（seq）

服务端给客户端发送自己的随机序列号（seq）和确认号（ack=客户端的seq+1），打上确认标记（ACK）

客户端再给服务端发送序列号（第一次握手生成的seq+1）和确认号（ack=服务端的seq+1）

## 四次挥手

客户端发送FIN请求（完成）和seq

服务端发送ack(seq+1)，打上确认标记（ACK）

客户端处理完所有未完成的事

服务端发送FIN请求和seq，ack，打上确认标记（ACK）

客户端进入超时等待状态（保证tcp可靠）

客户端发送确认请求（ACK）,关闭连接

## 缓存机制

**缓存存到的位置**：内存缓存memory cache/硬盘缓存disk cache

**强缓存**

浏览器强制缓存服务端提供的资源

Expires:固定时间

Cache-Control:max-age=时间戳（优先）

第二次发送请求资源直接从浏览器中读取缓存

**协商缓存 304**

Last-Modified:时间

If-Modified-Since:时间,对比与Last-Modified时间是否相等



ETag:标记/哈希，对比标记是否改变（优先）

If-no-match：对比与ETag是否相等

![img](https://18403581474.oss-cn-beijing.aliyuncs.com/img/202605251545825.png)

注：

- **不安全方法** →非**GET / HEAD**默认不可缓存，除非**显式允许**

  `Content-Location：url` 告诉缓存系统：**这个响应可以用这个 URI 作为缓存 key**。

## CORS 通信(解决跨域)

**简单请求**

- 请求方法是GET,POST,HEAD
- HTTP 的请求头信息不超出以下几种字段
  - Accept
  - Accept-Language
  - Content-Language
  - Last-Event-ID
  - Content-Type：只限于三个值`application/x-www-form-urlencoded`、`multipart/form-data`、`text/plain`

基本流程：

1. 浏览器直接发出 CORS 请求。具体来说，就是在头信息之中，增加一个`Origin`字段（发出请求的当前源）

2. 如果`Origin`指定的域名在许可范围内，服务器返回的响应，会多出几个响应头信息字段


**非简单请求**

非简单请求是那种对服务器提出特殊要求的请求，比如请求方法是`PUT`或`DELETE`，或者**`Content-Type`字段的类型是`application/json`**， 会在正式通信之前，增加一次 HTTP 查询请求，称为“预检”请求（option请求）（preflight）

基本流程：

1. 浏览器发现是一个非简单请求，就自动发出一个“预检”请求，要求服务器确认可以这样请求，预检”请求用的请求方法是`OPTIONS`，表示这个请求是用来询问的。头信息里面关键字段是`Origin`，表示请求来自哪个源

2. 服务器收到“预检”请求以后，检查请求头字段，确认允许跨源请求，就可以做出回应


## 浏览器输入url发生什么

从输入 URL 到页面显示，浏览器经历了 **DNS 查询**，先去浏览器缓存和本地host文件查找映射，如果没有再去DNS服务器上查找，找到对应服务器的ip地址，然后建立TCP连接，经过**TCP三次握手**，客户端向服务端发送随机序列号，服务端再向客户端发送随机序列号和确认号，客户端再向服务端发送随机序列号和确认号，确认连接，如果是HTTPS协议，还会再执行**TLS/SSL连接**进行加密，大致分为非对称加密和对称加密两个阶段，客户端先向服务端发送随机数，服务端向客户端发送随机数和服务器证书，客户端从证书里提取服务器公钥，双方根据公钥生成对应的会话密钥，之后的通信都会携带会话密钥进行通信，连接成功后客户端向服务器**发送HTTP请求报文**，包含请求头，请求体等，这里涉及到了**跨域**CORS和**缓存**的概念，HTTP请求大致分为简单请求和非简单请求，简单请求就是GET/POST/原始请求头的请求，非简单请求就是DELETE/PUT/非原始请求头的请求，在发送非简单请求时，浏览器会先发送一次预检请求OPTIONS，携带请求源，服务端去检查是否允许这个请求源跨域，如果允许则浏览器再发送正常请求，一般解决跨域的方法有后端加入请求源或使用CORS中间件，前端通过代理绕过跨域，浏览器缓存分为强缓存和协商缓存，请求后端时会先去检查是否命中强缓存，强缓存由Expires绝对时间和cache-control相对时间(优先)设置,如果在时间范围内就直接去缓存里找值，如果没用强缓存或时间过期了那就走协商缓存，协商缓存由Last-Modified最后修改时间和ETag文件hash值(优先)设置,如果和后端返回的响应头里对应属性值一样就返回304去缓存里找值，如果服务端修改过就正常请求，服务端收到请求后会向浏览器**发送响应报文**，包括响应头响应体等，当数据传输完毕，进行**TCP四次挥手**，由客户端发送 FIN，服务器回复 ACK ，此时客户端无法再向服务端发送请求，服务端把残余数据发送完后再发 FIN，最后客户端回复 ACK，完成四次挥手，连接彻底关闭。将文件数据等传回浏览器后，浏览器会对返回的数据进行渲染，服务端根据架构会有不同的返回情况，有**CSR,SSR,SSG,RSC**，客户端渲染是将html,css,js文件返回在客户端进行渲染，不利于SEO和首屏加载慢，服务端渲染会返回渲染好的html字符串，在客户端进行水合，首屏加载快，但交互功能会延迟，静态资源站点是直接部署到CDN，用离用户最近的边缘节点返回数据，RSC是可以局部水合提高性能，执行浏览器的**渲染流程**，先解析DOM树，COM树生成render树，进行布局，分层，绘制，再去合成线程上合成，回流是重新生成布局树，重绘是重新生成绘制指令，当渲染好后用户就看到了页面



## SSE

单工通讯，事件流，客户端与服务端建立持久化连接

1.EventSource 只支持 GET

```js
 document.addEventListener('keydown',(e)=>{
      if(e.key === 'Enter'){
        let sse = new EventSource('http://localhost:3000/api/sse')//后端接口
        sse.addEventListener('message',(e)=>{//事件类型，可以后端自定义名称
          document.getElementById('m').innerHTML+=e.data
        })
      }
    })
```

```js
app.get("/api/sse", function (req, res) {
  res.writeHead(200, {
    "Content-Type": "text/event-stream",
  });//核心设置请求头
  let text = fs.readFileSync("./1.txt").toString();
  let arr = text.split("");
  let cnt = 0;
  let t = setInterval(() => {
    if (cnt < arr.length) {
      res.write(`event:自定义事件名称`)
      res.write(`data:${arr[cnt]}\n\n`);// 两个\n，sse的消息格式
      cnt++;
    } else {
      clearInterval(t);
    }
  }, 300);
});
```

2.**Fetch + ReadableStream**

```js
async function streamFetch() {
  const response = await fetch('/api/stream', { method: 'POST' });
  const reader = response.body.getReader();
  const decoder = new TextDecoder();
  
  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    
    const chunk = decoder.decode(value, { stream: true });
    console.log('收到块:', chunk);
    document.getElementById('output').innerHTML += chunk;
  }
}
```

```js
app.post('/api/stream', async (req, res) => {
  res.setHeader('Content-Type', 'text/plain; charset=utf-8');
  res.setHeader('Transfer-Encoding', 'chunked');  // 关键：分块传输
  
  const text = '这是一段需要流式输出的长文本...';
  
  for (const char of text) {
    res.write(char);           // 逐字写入
    await new Promise(r => setTimeout(r, 50));  // 模拟生成延迟
  }
  
  res.end();
});
```



## WebSocket

全双工通讯

握手完成后升级为 TCP 长连接

```js
let ws = new WebSocket("ws://localhost:8080");
    ws.addEventListener("open", (e) => {
      console.log("连接");
    });
    ws.addEventListener("message", (e) => {//收到服务端消息时执行
      渲染页面代码
    });
//open，message，close,error
```

```js
const { WebSocketServer } = require("ws");
// 创建 WebSocket 服务器，监听 8080 端口
const wss = new WebSocketServer({ port: 8080 });
wss.on("connection", (ws) => {
  console.log("客户端已连接");

  // 监听客户端发送的消息
  ws.on("message", (message) => {
    console.log("收到消息:", message.toString());
    // 广播消息，群发（聊天室）
    //wss.clients 保存所有连接的客户端，set
    wss.clients.forEach((client) => {
      client.send(`广播消息: ${message}`);
    });

    // 发送响应给客户端，单发
    // ws.send(`返回给客户端: ${message}`);
  });

  // 监听客户端断开连接
  ws.on("close", () => {
    console.log("客户端断开连接");
  });
  
  //心跳监测，监测保持连接
  let heartInterval = null;
  let heartCheck = () => {
    if (ws.readyState === ws.OPEN) {
      ws.send(JSON.stringify({ type: state.HEART, message: "心跳检测" }));//发送响应到客户端
    } else {
      clearInterval(heartInterval);
    }
  };
  heartInterval = setInterval(heartCheck, 5000);
});
console.log("WebSocket 服务器运行在 ws://localhost:8080");

```



## **HTTP 轮询/长轮询**

半双工通讯

例：扫码登陆，**前端**网页不知道用户扫没扫，于是不断去向**后端**服务器询问，看有没有人扫过这个码

轮询：不断去请求服务端

长轮询：设置更长的超时时间，在时间内只要服务器收到了请求，就立马返回给客户端。减少了 HTTP 请求的个数，如果超时，那就立马发起下一次请求

## navigator.sendBeacon

网络请求方法，发送心跳包，埋点，用户反馈

异步，可跨域，ping请求

```js
navigator.sendBeacon("http://localhost:3000/api/b", json);
```

## HTTP

| 对比项       | HTTP                        | HTTPS                                                        |
| ------------ | --------------------------- | ------------------------------------------------------------ |
| **全称**     | HyperText Transfer Protocol | HyperText Transfer Protocol **Secure**                       |
| **端口**     | 80                          | 443                                                          |
| **安全性**   | 明文传输，易被窃听/篡改     | 加密传输，防窃听、防篡改、防冒充                             |
| **加密方式** | 无                          | SSL/TLS 协议加密: 对称加密（规定规则），非对称加密（公钥私钥） |
| **证书**     | 不需要                      | 需要 SSL 证书（由 CA 机构颁发）                              |
| **性能**     | 快（无加密开销）            | 稍慢（有握手和加密开销，但可优化）                           |
| **SEO**      | 无优势                      | 搜索引擎优先收录                                             |

HTTPS通信过程：TCP 三次握手 → SSL/TLS 握手（交换证书、协商密钥）→ 加密 HTTP 通信

SSL/TLS握手过程：

版本，随机数，加密算法->确认版本，随机数，确认加密算法，服务器证书->证书提取公钥，摘要数据返回服务端做校验->生成会话密钥，摘要数据返回客户端做校验 

**状态码**

**请求头**

**响应头**

## JWT

```js
//1.html
fetch("http://localhost:3000/api/login", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          username: nameValue,
          password: pwdValue,
        }),
      }).then((res) => res.json()).then(res=>{
        localStorage.setItem("token", res.token);
        location.href = "http://127.0.0.1:5501/demo/2.html";
      }).catch((data) => {
        console.log(data);
      });
    });
//2.html
fetch("http://localhost:3000/api/list",{
      headers:{
        Authorization: localStorage.getItem("token"),
      }
    })
      .then((res) => res.json())
      .then((res) => {
        console.log(res);
      })
      .catch((data) => {
        console.log(data);
      });
```



```js
const express = require("express");
const jwt = require("jsonwebtoken");
let app = express();
const cors = require("cors");
app.use(cors());
app.use(express.json());
app.use(express.urlencoded({ extended:false }));
let user = {
  username: "admin",
  password: '123456',
};
let key = "AAA";
app.post("/api/login", function (req, res) {
  let { username, password } = req.body;
  if (username === user.username && password === user.password) {
    let token = jwt.sign({ username }, key, { expiresIn: "1h" });
    res.json({
      code: 200,
      msg: "登录成功",
      token,
    });
  } else {
    res.status(401).json({
      code: 401,
      msg: "用户名密码错误",
    });
  }
});
app.get("/api/list", function (req, res) {
  let token = req.headers["authorization"];
  jwt.verify(token, key, function (err, decoded) {
    if (err) {
      res.status(403).json({
        code: 403,
        msg: "token过期",
      });
    } else {
      res.json({
        code: 200,
        msg: "获取数据成功",
        data: [
          { id: 1, name: "张三" },
          { id: 2, name: "李四" },
          { id: 3, name: "王五" },
        ],
      });
    }
  });
});
app.listen(3000, () => {
  console.log("Server is running at http://localhost:3000");
})
```

### Token 无感刷新的竞态处理

 Token 过期时，同时有多个请求发出

- **锁（isRefreshing）**：保证只有一个刷新请求
- **队列（refreshQueue）**：其他请求排队等待，不重复刷新
- **广播**：刷新成功后，用新 Token 重试所有排队请求

## 网络状态

online在线,offline离线

navigator.connection获取网络连接属性（effectiveType网速）

## XSS

跨站脚本攻击

- 反射性xss：有script标签的url

- 存储型xss：根据评论等把脚本注入数据库

- DOM型xss：注入与html有交互性的js代码，innerHTML,v-html,eval

  把<,>替换成&lt,&gt，现代框架自动防御，富文本场景：白名单过滤

注：

- 浏览器解析 HTML 的优先级：**标签 > 文本**
- 浏览器解析 `innerHTML` 时，会**识别并忽略**其中的 `<script>` 标签，这是内置的安全防护，但其他攻击方式仍然有效，<img src="x" onerror="alert('XSS')">

## CSRF

跨站请求伪造

Cookie 保存在浏览器，恶意网站自动向银行发请求，浏览器自动带 Cookie ，银行认为是合法操作，攻击成功

| 方案                    | 原理                               | 实现                                                         |
| ----------------------- | ---------------------------------- | ------------------------------------------------------------ |
| **CSRF Token**          | 服务端生成随机 Token，请求必须携带 | 表单隐藏域 / 请求头<br />1.  Token存入 Session + 嵌入表单/JS <br />2. 用户提交请求携带 Token → 服务端比对 Session 中的 Token <br />3. 一致则通过，不一致则拒绝 |
| **SameSite Cookie**     | 设置Cookie 不随跨站请求发送        | `Set-Cookie: session=xxx; SameSite=Strict`                   |
| **Referer/Origin 校验** | 检查请求来源是否合法               | 服务端比对白名单                                             |
| **双重 Cookie 验证**    | token同时存 Cookie 和请求参数里    | Cookie 自动携带 + 自定义请求头的特性，比对一致性(设置自定义请求头则非简单请求受 CORS 限制) |
