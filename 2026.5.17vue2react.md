# Vue to React 转换工具

一个自动将Vue.js组件转换为React.js组件的工作流工具。

## 功能特性

- 📁 支持读取Vue项目目录结构和指定文件
- 🔍 解析Vue组件（模板、脚本、样式）
- 🔄 模板语法转换（v-bind, v-model, v-for, v-if等）
- 🪝 Composition API转换（ref, computed, watch）
- ⏱️ 生命周期转换（mounted, unmounted等）
- 📊 差异对比报告
- ⚠️ 错误提示和修复建议

## 支持的转换

### 模板语法

convert_template

| Vue | React |
|-----|-------|
| `:prop="value"` | `prop={value}` |
| `v-model="val"` | `value={val} onChange={setVal(...)}` |
| `v-for="item in list"` | `{list.map((item) => (...))}` |
| `v-if="cond"` | `{cond && (...)}` |
| `@click="handler"` | `onClick={handler}` |
| `<slot/>` | `{children}` |

### 组合式API

convert_composition

| Vue | React |
|-----|-------|
| `const x = ref(val)` | `const [x, setX] = useState(val)` |
| `const y = computed(() => ...)` | `const y = useMemo(() => ..., [])` |
| `watch(x, (n, o) => ...)` | `useEffect(() => {...}, [x])` |

### 生命周期

convert_lifecycle

| Vue | React |
|-----|-------|
| `mounted` | `useEffect(() => {...}, [])` |
| `beforeMount` | `useLayoutEffect(() => {...}, [])` |
| `unmounted` | `useEffect(() => {return () => {...}}, [])` |

## 使用方法

```python
from src.graphs.graph import main_graph

# 输入参数
input_data = {
    "input_path": "/path/to/YourComponent.vue",
    "output_dir": "/path/to/output"
}

# 执行工作流
result = main_graph.invoke(input_data)
```

## 工作流结构

工作流定义LangGraph 负责

- LangGraph 会自动处理状态传递，节点间通过 `state` 共享数据

- 协调 LLM 调用顺序
- 处理上下文传递
- 管理重试逻辑
- 收集和汇总结果

```
read_directory读取目录，读取指定的 Vue 文件或目录 -> 
parse_vue解析 Vue，提取 template 内容，区分 <script setup> 或 Options API，提取 imports、refs、生命周期钩子样式 - 提取 style 内容    -> 
convert_template 模板语法转换，将 Vue 模板语法转换为 React JSX-> 
convert_composition 组合式 API 转换，将 Vue Composition API 转换为 React Hooks-> 
convert_lifecycle 生命周期转换，Vue 生命周期 → React useEffect/useLayoutEffect-> 
generate_react 生成 React 代码-> 
diff_compare 差异对比，逐行差异（新增、删除、修改），相似度评分（基于文本重叠），差异汇总统计-> 
error_report 收集所有错误和警告，生成修复建议
```

