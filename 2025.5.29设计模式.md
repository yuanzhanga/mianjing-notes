## 设计模式

#### 工厂模式

调用函数返回一个对象

#### 单例模式

保证只有一个对象，重复调用获取的是同一个对象

```js
class SingleTon {
  static #instance
  static getInstance() {
    if (!this.#instance) {
      this.#instance = new SingleTon();
    }
    return this.#instance;
  }
}
```

#### 观察者模式

一对多，一个对象状态改变，所有依赖的对象自动收到通知

#### 发布订阅模式

有中间商

```js
  class EventBus {
    constructor() {
      this.events = {};
    }
    //注册
    on(e, fn) {
      if (!this.events[e]) this.events[e] = [];
      this.events[e].push(fn);
    }
    //清空
    off(e, fn) {
      //如果没有传入fn，则清空该事件的所有监听函数
      if (!fn) {
        this.events[e] = undefined;
        return;
      }
      if (this.events[e]) {
        this.events[e] = this.events[e].filter((cb) => {
          return cb !== fn;
        });
      }
    }
    emit(e, ...arg) {
      if (this.events[e]) {
        this.events[e].forEach((cb) => {
          cb.apply(this, arg);
        });
      }
    }
    once(e, fn) {
      const onceFn = (...arg) => {//箭头函数
        fn.apply(this, arg);
        this.off(e, onceFn); //执行一次后移除监听
      };
      this.on(e, onceFn);
    }
  }
```

#### 原型模式

复制一个已经存在的实例来返回新的实例,而不是新建实例

Object.create

#### 代理模式

缓存代理

#### 迭代器模式
