react项目各个文件的作用

tsconfig作用

生产依赖和开发依赖区别

public,assets文件夹有什么区别

**`assets` 里的文件会被构建工具压缩、加 hash、转格式、内联等优化，让网站加载更快、缓存更智能；`public` 里的文件原样复制，不做任何处理。**

项目怎么修改端口号

gui和cli的区别

**GUI 是"点点点"的图形界面，直观但慢；CLI 是"敲命令"的文本界面，高效但需学习。两者互补，开发者通常 GUI 写代码，CLI 做自动化。**

this面试题 window,node环境,严格模式

**严格模式下只是this不指向全局对象，但全局对象依然存在**



图片写入框

useMessage mcp skill 

agent执行流程，functioncall,tool的区别

AG-UI（自动化操作无 API 系统）  MCP（Agent-工具）、A2A（Agent-Agent）三层协议栈

ts和ai结合

看一些agent相关的东西

最好写一个比较小的agent loop，可以去看看一些开源的框架，比如说TS的叫Mastral。或者什么OpenCode这种相关的，GitHub上有很多

 JavaScript AI 开发全栈：Vercel AI SDK（UI 层）、LangChain.js（后端编排）、Zod（结构化输出）、pgvector/Qdrant（向量存储）、Langfuse（可观测性），是前端团队切入 AI 开发的权威参考地图  [AI Development Stack for JavaScript 2026 — PkgPulse Guides](https://www.pkgpulse.com/guides/ai-development-stack-javascript-2026)