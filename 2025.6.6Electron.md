# electron

app,窗口,主进程，渲染进程，预处理进程

```js
app.requestSingleInstanceLock() 请求获取应用程序的单实例锁
若成功获取,会是 true ，表明当前应用是唯一实例；若失败， 为 false ，意味着已经有该应用的另一个实例在运行
```

```js
const store = new Store()//持久化储存
get/set/delete/clear/has/
onDidChange('StoreName',callback)
//保存到本地用户指定配置文件里，数据默认存储在系统应用数据目录
```

```js
//主进程
import { app, session, ipcMain, BrowserWindow, Tray} from "electron";
function createWebWindow() {
    let win = new BrowserWindow([options])//webPreferences里preload
    //win.webContents 代表这个窗口的网页内容对象
    win.on('event名字',回调)//ready-to-show窗口渲染完成即将显示时
    /*通信*/
    ipcMain.on('send事件名',回调)
    ipcMain.on('invoke事件名',回调)
    /*通信*/
    win.loadFile(indexHtml);
    return win
}
app.on('event名字',/*回调*/{
    createWebWindow()
})//ready准备阶段/activate激活/before-quit退出应用前/window-all-closed全部关闭
```

```js
//预处理进程
import { ipcRenderer, contextBridge } from "electron";
contextBridge.exposeInMainWorld("对象名称/信道", {
    //对象属性方法
    mysend() {//单向
    return ipcRenderer.send('send事件名', ...arg);
  }
    myinvoke() {//返回promise //双向
    return ipcRenderer.invoke('invoke事件名');
  }
})
```



```js
Tray 是一个模块，它允许开发者在操作系统的系统托盘创建一个图标,进行交互
const tray = new Tray("resources/icon.png");
// 设置鼠标悬停在托盘图标上时显示的提示信息
tray.setToolTip("111");
```

```js
session 代表浏览器会话，它负责管理网络请求相关的数据，像 Cookie、缓存、代理设置等。每个 BrowserWindow 都关联着一个 session ，借助 session 能对网络请求和响应进行细粒度控制。 session.defaultSession 是一个全局默认的会话实例，所有未指定 session 的 BrowserWindow 都会使用这个默认会话。
```

