发5-6个sse接口，服务器rpc有限，第七个接收不了

停止-用户体验？

多session怎么做

工作流，vue->react

vue2中声明式生命周期，state在onmounted里循环set,react理解成状态的重复依赖，对不准

webpack的loader,插件

loader在什么生命周期阶段触发

插件通过什么原理介入到webpack生命周期里

webpack的生命周期是什么，怎么实现的

写了文件类型，编译阶段通过loader解析成ast

插件在很多生命周期里使用是什么机制

ts的泛型，什么场景下会用到

搜索框发送请求的数量是不会变，abortcontroll只在前端层面取消丢弃请求，后端还是要处理，让后端做mid，发送只要有新hash值就丢弃之前的行为

工程化，AI

