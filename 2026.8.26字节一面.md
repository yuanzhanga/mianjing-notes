# 2026.8.26 字节一面

## 1. minivue 关键技术实现

**编译阶段（模板 → 渲染函数）**：

```
模板字符串 → 词法分析 parse() → Token 序列
  → 语法分析 → AST（含 type, tag, props, children）
  → transform 优化：文本合并（相邻静态文本合并）、静态提升（hoistStatic 提取到渲染函数外）、补丁标记（patchFlag 标记动态部分）、树结构扁平化
  → codegen() 生成 render 函数字符串
  → new Function() 得到可执行 render 函数
```

**运行阶段**：执行 render(_ctx) → 返回 VNode 树 → patch(null, VNode) 创建真实 DOM；响应式数据变化 → 重新执行 render 得新 VNode → patch(old, new) diff 最小化更新真实 DOM。

**核心机制**：

- **reactive**：`new Proxy(raw, { get, set })`。get 时 `dep.depend()` 收集依赖（effectWatch 用 currentEffects 记录正在执行的函数），set 时 `dep.notice()` 执行所有依赖
- **ref** 底层也是 reactive：基本类型无法用 Proxy 代理，包装成 `{ value }` 对象再套 Proxy，render 时解包
- **computed/watch** 本质都是响应式（ReactiveEffect 系统）：computed 加了缓存（布尔值判断是否修改）；watch 异步批量更新——依赖函数数组放进 nextTick 的 pre-flush 队列，下一个事件循环统一执行
- **nextTick**：把回调放进 Promise.then，等本轮事件循环的 DOM/数据更新完再执行
- **createApp(App).mount()**：先执行 setup 拿数据，effectWatch 包 render（数据变自动重跑），首次 mountElement，之后走 diff
- **diff 算法**：同层比较——tag 不同直接 replaceWith 重建节点；props 对比增删（新有旧无则加、旧有新无则删）；children 分四种情况（新串旧串 / 新串旧数组 / 新数组旧串 / 新数组旧数组，数组取 min 长度逐对 diff，多出来的挂载或移除）
- **组件 = 对象**：有 props/emit/slots/parent/provides 属性，provide 通过 `parent ? parent.provides : {}` 沿原型链找祖先，靠 parent 连接实现父子通信
- **diff 优化**：双端对比、标记静态节点（跳过静态内容）

来源：`mianjing-notes/2025.5.12mini-vue.md`、`mianjing-notes/项目总结.md`

## 2. markdown 为什么自研，怎么实现的

**怎么实现**（项目总结.md）：

markdown 语法转 html：`词法分析 → token 数组 → 语法分析 → AST → 渲染成 html 标签结构`

1. 正则匹配各种样式：标签、代码块、段落，再细分匹配斜体、粗体、链接、普通文本
2. 匹配完返回 token 流（AST 语法树），按类型（标签/代码块/段落…）递归返回对应的 html 字符串
3. **html 特殊字符转义**：防止恶意脚本注入（XSS），同时确保内容正确显示而不是被浏览器解析为标签
4. 可配合 remark/rehype 插件体系

**为什么自研**：文档里没有直接记录原因，但能答的方向（依据：转义防 XSS 是实现里被强调的重点，且该功能属于类 GPT 项目的二期功能，AI 生成内容不可信）——安全可控：AI 输出的 markdown 需要逐 token 解析、逐字符转义，自研渲染管线能控制哪些标签放行、哪些转义，而不是第三方库黑盒处理。

来源：`mianjing-notes/项目总结.md`（markdown渲染 段）、`2026.4.28美团AI面.md`

## 3. es6 的新特性

| 类别 | 特性 |
|---|---|
| 声明 | let / const（块级作用域） |
| 语法糖 | 解构赋值、模板字符串、展开运算符 `{...obj}`、箭头函数、class |
| 数据结构 | Map / Set / WeakMap / WeakSet、Symbol、BigInt |
| 对象增强 | 属性简写、Object.assign / keys / values / entries / fromEntries / is |
| 异步 | Promise、async / await |
| 代理反射 | Proxy（13 种拦截）、Reflect |
| 迭代 | for...of、迭代器协议、Generator |
| 模块化 | import / export |

来源：`数据结构.md`、`2025.4.1对象.md`、`2025.4.13Proxy.md`、`2025.4.18Promise.md`、`2025.4.19闭包.md`、`2025.4.14类.md`

## 4. 逐个特性展开

- **let**：块级作用域，`{}` 内生效，有暂时性死区（2026.4.28美团AI面.md 作用域段）
- **模板语法**：反引号 + `${}` 插值。在 minivue 里模板字符串是编译入口：模板 → 词法分析 → AST（项目总结.md）
- **Map / Set**：引用类型。Map 键可以是任意类型；Set 值唯一。手写 Set：用对象存 items，add 前 has 判重，返回 this 链式调用（2025.4.13Proxy.md 手写 Set 类）
- **WeakMap / WeakSet**：**弱引用**——key 只能是对象，被引用的对象如果没有其他引用，可以被垃圾回收，不阻止 GC，防止内存泄漏（2025.4.13Proxy.md 弱引用段）
- **Proxy**：13 种拦截操作：get/set/has/deleteProperty/ownKeys/getOwnPropertyDescriptor/defineProperty/preventExtensions/getPrototypeOf/isExtensible/setPrototypeOf/apply/construct，配合 Reflect 对应方法使用（2025.4.13Proxy.md）
- **迭代器**：可迭代协议，for...of 消费。流式输出里 `reader.read()` 返回 `{ done, value }` 就是迭代器模式的应用（项目总结.md）
- **Promise**：手写实现 = 状态机（Pending/Fulfilled/Rejected）+ resolveCallbacks/rejectCallbacks 回调数组 + `queueMicrotask` 保证异步执行 + then 返回新 Promise 实现链式（2025.4.18Promise.md）。await 是让出线程的标志：await 后的函数先执行，跳出 async 函数执行外面的同步代码，等本轮事件循环后再回来取 Promise 的 resolve 值
- **箭头函数**：① this 继承外层代码块 ② 不能 new（不能当构造函数）③ 没有 arguments（用 rest 参数代替）④ 不能 yield（不能做 Generator）⑤ call/apply/bind 无法改 this（2025.4.19闭包.md）
- **class**：extends 继承 + super。class 机制还是在 prototype 基础上封装：constructor 执行赋值 → Object.defineProperty 把方法挂到原型上 → Object.create / Object.setPrototypeOf 实现类继承（子类原型 `__proto__` 指向父类原型、子类构造函数 `__proto__` 指向父类构造函数）→ 变更子类 this 作用域实现 super（2025.4.14类.md、2025.4.15原型.md）
- **原型链**：`p.__proto__` → `Person.prototype` → `Object.prototype` → null。一切对象最终继承 Object.prototype；Function 原型链 `Function → Function.prototype → Object.prototype → null`，Object 和 Function 互相继承。instanceof = 沿 `__proto__` 链向上找构造函数的 prototype（2025.4.15原型.md 手写 new_instance_of）

## 5. 什么是宏任务，微任务

笔记定义（2025.4.23字节一面.md）：**消息队列：宏任务，微任务（高优先级）**。

结合事件循环机制（2025.4.7事件循环.md + 标准知识）：

- **宏任务**：消息队列中的任务。script 整体、setTimeout/setInterval、I/O、UI 渲染。每次循环从消息队列取出**第一个**宏任务执行
- **微任务**：高优先级队列。`promise.then/catch/finally`、`queueMicrotask`、MutationObserver。**当前宏任务执行完后、下一个宏任务开始前，清空所有微任务**（包括微任务里新产生的微任务）
- **为什么要区分**：微任务让 Promise 回调在宏任务结束后立刻执行，不必等整个消息队列轮转一圈；且每轮渲染前微任务会被清空，保证状态是新的
- **实例**（2026.7.20百度一面.md）：

```js
const p1 = new Promise((resolve) => {
    console.log(0)
    resolve()
}).then(function f1() {
    console.log(1)
    const p2 = new Promise(resolve => {
      console.log(2)
      resolve()
    }).then(function f3() {
      console.log(3)
    }).then(function f4() {
      console.log(4)
    })
}).then(function f2() {
    console.log(5)
})
console.log(6)
// 输出 0612354：先 f3 再 f2 再 f4（微任务队列按注册顺序出队）
```

## 6. 介绍一下 aireview 的架构设计

核心目标：让 AI 代码审查发生在**编码时**而不是提 MR 后（shift-left）。

**v1**：远端 GitLab，通过 GitHub Action 在提 MR 时审查本次改动并回写评论。问题：产生幻觉、问题发现晚。

**v2（当前）**：本地 MCP Server 架构

1. 在 `.cursor/mcp.json` 中以 stdio 方式（npm 命令）启动一个 **MCP Server**，把审查能力封装成两个 MCP Tool 注册上去，Cursor 里的 Agent 像调本地函数一样触发审查
2. **两个 MCP Tool**（Agent loop 循环判断用哪个）：
   - `cr_shift_left_batch_review(session_id)`：服务端根据当前上下文自动拉 diff（GitLab API 或本地 git diff），发起批量审查会话，返回结构化问题列表
   - `cr_shift_left_post_batch_results(session_id, batch_results)`：回写结构化结果（文件路径、行号、严重程度 1–5、问题分类、描述、原代码片段、建议修改代码），返回一个网页展示所有可修改问题
3. **"规则引擎 + 大模型"分层**：规则层（CursorRules 声明项目背景/用例模板/API 模式/命名规范 → 全局生效、团队共享、强约束；ESLint 调优、Prompt 调优减少噪音误报）管命名规范、敏感信息等静态检查；模型层做逻辑合理性、最佳实践的语义审查
4. **鉴权**：GitLab Personal Access Token，检测 Token 是否拥有项目访问权限后回写代码
5. **优化（Skill 化）**：输入 aicr 命中 skill 执行脚本，把 mcp 工具逻辑集成进脚本，可本地运行，不需要重复提交 MR

**未解决问题**：单次 MR / 工作区变更很大时如何切分——见下一题分桶。

来源：`mianjing-notes/AI.md`（AICR 段）、`2026.8.20百度一面.md`、`2026.8.28百度二面.md`

## 7. 分桶 skill，是不是分完又没有完整的上下文了

**是，简单分桶会丢上下文**：每个 agent 只看到自己桶里的 diff，跨文件依赖和全局约束（比如"这次改动统一换了接口签名"）就丢了。

**笔记里的方案**（AI.md，分两步解决）：

1. **先补全局上下文**：先让 AI 分析依赖关系，生成**变更说明书 / 全局上下文**
2. **再分桶并发**：按文件类型 / 模块分桶，保证**桶之间互不依赖**；通过 MCP 确认分桶情况；用 workflow 启动 N 个 Agent 并发审查（每个 agent 拿着全局说明书 + 自己桶的 diff）；最终主 Agent 去重拿结果，格式化输出报告

**极端情况**（2026.8.25快手一面.md："极端情况下所有文件都有依赖关系，如何分桶"）：依赖关系本身写进变更说明书，按说明书声明的边界划分——强相关的文件放进同一桶（或同一个 agent 的上下文），全局约束放说明书里每个 agent 都带一份，桶内审查、桶间靠说明书对齐。

来源：`mianjing-notes/AI.md`、`2026.8.25快手一面.md`

## 8. 版本号的排序 [1.2, 1.1.1.1, 2.45]

结果：`['1.1.1.1', '1.2', '2.45']`（1.1.1.1 < 1.2 < 2.45）

两种实现（见 `bl-note/demo/1.js`）：

```js
// 方式一：localeCompare + numeric（必须显式开 numeric，否则是字典序，1.10 会排到 1.2 前面）
arr.sort((a, b) => a.localeCompare(b, undefined, { numeric: true }))

// 方式二：手写算法，按 . 分段逐段比数值
function compareVersion(a, b) {
  const pa = a.split('.')
  const pb = b.split('.')
  const len = Math.max(pa.length, pb.length)
  for (let i = 0; i < len; i++) {
    const na = Number(pa[i]) || 0   // 缺的段补 0，所以 1.1 < 1.1.1.1
    const nb = Number(pb[i]) || 0
    if (na !== nb) return na - nb   // sort 只看正负号
  }
  return 0
}
```

## 9. 柯里化函数的实现

笔记版（2025.4.19闭包.md，闭包 + 函数上挂取值器）：

```js
function sum(n) {
  let total = n
  function inner(m) {
    total += m
    return inner
  }
  inner.value = () => total
  return inner
}
sum(1)(2)(3).value() // 6
```

通用版（把一个多参函数变成可分批传参）：

```js
function curry(fn) {
  return function curried(...args) {
    if (args.length >= fn.length) return fn(...args)
    return (...rest) => curried(...args, ...rest)
  }
}
const add = curry((a, b, c) => a + b + c)
add(1)(2)(3) // 6
add(1, 2)(3) // 6
```

原理：柯里化靠闭包保存已收集的参数，参数收齐前一直返回新函数，收齐后执行原函数。
