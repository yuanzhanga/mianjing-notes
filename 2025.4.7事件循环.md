# 事件循环

渲染进程启动后，会开启一个**渲染主线程**，主线程负责执行 HTML、CSS、JS 代码，每秒把页面画 60 次

JS是一门单线程的语言，这是因为它运行在浏览器的渲染主线程中，而渲染主线程只有一个。

而渲染主线程承担着诸多的工作，渲染页面、执行 JS 都在其中运行。

如果使用同步的方式，就极有可能导致主线程产生阻塞，从而导致消息队列中的很多其他任务无法得到执行。这样一来，一方面会导致繁忙的主线程白白的消耗时间，另一方面导致页面无法及时更新，给用户造成卡死现象。

所以浏览器采用异步的方式来避免。具体做法是当某些任务发生时，比如计时器、网络、事件监听，主线程将任务交给其他线程去处理，自身立即结束任务的执行，转而执行后续代码。当其他线程完成时，将事先传递的回调函数包装成任务，加入到消息队列的末尾排队，等待主线程调度执行。

在这种异步模式下，浏览器永不阻塞，从而最大限度的保证了单线程的流畅运行。



每次循环从消息队列中取出第一个任务执行，而其他线程只需要在合适的时候将任务加入到队列末尾即可。



1. 浏览器实现计时器时，如果嵌套层级超过 5 层，则会带有 4 毫秒的最少时间，这样在计时时间少于 4 毫秒时又带来了偏差
2. 受事件循环的影响，计时器的回调函数只能在主线程空闲时运行，因此又带来了偏差



# 渲染步骤

当浏览器的网络线程收到 HTML 文档后，会产生一个渲染任务，并将其传递给渲染主线程的消息队列。

在事件循环机制的作用下，渲染主线程取出消息队列中的渲染任务，开启渲染流程。

-------

整个渲染流程分为多个阶段，分别是： HTML 解析、样式计算、布局、分层、绘制、分块、光栅化、画

每个阶段都有明确的输入输出，上一个阶段的输出会成为下一个阶段的输入。

这样，整个渲染流程就形成了一套组织严密的生产流水线。

布局、绘制在 CPU，光栅化和合成在 GPU

```plain
opacity,transform在Composite线程
```

![img](https://18403581474.oss-cn-beijing.aliyuncs.com/img/202504221200088.png)



浏览器单线程模型（每个标签页）：
┌─────────────────────────────────────┐
│           主线程（JS + GUI）          │
│  ┌─────────┐    ┌─────────────┐     │
│  │  JS 执行 │ ←→ │ GUI 渲染    │     │
│  │  (V8)   │    │ (样式/布局/绘制)│    │
│  └─────────┘    └─────────────┘     │
│        ↑ 互斥锁，不能同时执行          │
└─────────────────────────────────────┘



`will-change: transform` **不能滥用**。它会强制创建独立的合成层，消耗额外的 GPU 内存。如果给所有元素都加，会导致内存暴涨、合成开销增大，反而降低性能。应仅在需要动画的元素上临时添加，动画结束后移除

**Layout Thrashing（布局抖动）**是指在同一帧中交替读取布局属性（如 `offsetWidth`）和修改布局样式（如 `width`），导致浏览器被迫反复计算布局。应批量读取、批量写入

# 进程和线程的区别

**进程是资源分配的最小单位，线程是CPU调度的最小单位。**

**进程是资源容器，线程是执行实体。**

| 维度         | 进程（Process）                      | 线程（Thread）                   |
| ------------ | ------------------------------------ | -------------------------------- |
| **本质**     | 一个运行中的程序                     | 进程内的一条执行流               |
| **资源**     | 拥有独立的地址空间、内存、文件句柄   | 共享所属进程的资源               |
| **开销**     | 创建/销毁/切换开销大（需要切换页表） | 开销小（只需切换寄存器和栈）     |
| **通信**     | 需要 IPC（管道、Socket、共享内存等） | 直接读写共享内存，简单但需同步   |
| **崩溃影响** | 一个进程崩溃不影响其他进程           | 一个线程崩溃可能导致整个进程崩溃 |
| **数量**     | 一个程序可以多个进程                 | 一个进程可以多个线程             |

比线程更轻量的存在：**协程**

**线程资源共享**

| 资源类型        | 共享？     | 说明                                    |
| --------------- | ---------- | --------------------------------------- |
| 堆内存          | ✅ 共享     | `new`/`malloc` 出来的，所有线程都能访问 |
| 全局变量        | ✅ 共享     | 直接读写，需要锁保护                    |
| 打开的文件      | ✅ 共享     | 一个线程 `close`，其他线程受影响        |
| 代码段          | ✅ 共享     | 执行同一份指令                          |
| **栈**          | ❌ **私有** | 函数调用链、局部变量，互不干扰          |
| **寄存器**      | ❌ **私有** | 保存当前执行上下文                      |
| **程序计数器**  | ❌ **私有** | 记录执行到第几条指令                    |
| TLS线程本地存储 | ❌ **私有** | `thread_local` 变量，每个线程一份       |

**进程通信**

线程共享堆和全局变量，直接读写就能通信，但要加锁防止竞态

进程地址空间隔离，必须通过管道、消息队列、共享内存、Socket 等操作系统提供的 IPC 机制通信，其中共享内存最快，Socket 最通用。
