## 类gpt

**多轮对话**

**大文件分片上传**

分片文件上传 - 大文件分割上传，带进度显示

**虚拟滚动**

**语音转文字STT**

1. SpeechRecognition   onresult方法

   注意单例引用

2. TEN-Agent开源框架

   ```
   ┌─────────────────────────────────────┐
   │           TEN-Agent 应用层            │
   │  ┌─────────┐ ┌─────────┐ ┌────────┐ │
   │  │  ASR    │ │  LLM   │ │  TTS   │ │
   │  │ 语音识别 │ │ 大模型  │ │语音合成 │ │
   │  └────┬────┘ └────┬────┘ └───┬────┘ │
   │       └─────────────┴──────────┘      │
   │              业务逻辑编排               │
   ├─────────────────────────────────────┤
   │           TEN Framework               │
   │         （实时通信框架层）              │
   ├─────────────────────────────────────┤
   │              WebRTC                  │
   │    ┌─────────┐    ┌─────────┐      │
   │    │ 音视频采集 │    │ 网络传输  │      │
   │    │ getUserMedia│   │  ICE/SRTP│      │
   │    └─────────┘    └─────────┘      │
   │    ┌─────────┐    ┌─────────┐      │
   │    │ 音频处理 │    │ 视频编解码 │      │
   │    │ AEC/NS/AEC3│   │ VP8/H.264│      │
   │    └─────────┘    └─────────┘      │
   └─────────────────────────────────────┘
   ```

**TTS**

**Tool Call**

调用工具来增强回复

**流式输出**

SSE/ReadableStream

```js
const reader = response.body.getReader()//read/cancel/releaseLock
const { done, value } = await reader.read()//迭代器
decoder.decode(value, { stream: true }//解析
```

**"多轮对话 × SSE 长连接"** 与 **"有限 RPC 连接池"** 的冲突

WebSocket+SSE降级( 连接池或多路复用)

- WebSocket 替代 SSE
- SSE多路复用：请求同一个SSE接口，返回数据带dialogId分辨。服务器内部同时跑两个 AI 流，输出时穿插推送，前端按 ID 分拣，一条 SSE 数据量太大可以开 2-3 条 SSE 做负载均衡
- SSE连接池：维护一个队列，在队列里排序执行请求，达到高并发显示排队中，后端用Promise + 队列管理并发来限制 RPC 并发数，实现队列和并发控制
- 前端 Fetch API + ReadableStream实现SSE流式请求处理，React Hook + 状态管理 + AbortController手动取消请求

**记忆功能**

1. 短记忆，记录上下文到message数组里，每次请求带上所有上下文，`SummaryBufferMemory` 
2. **长记忆**，Rag,向量数据库

上下文记忆系统 — 记忆架构设计                                           

 │ │    构建分层记忆体系：                                                       

│ │    短记忆：通过 message 数组维护多轮上下文，每次请求携带完整对话历史。      

│ │    长记忆：基于 RAG + 向量数据库实现持久化知识检索，突破上下文长度限制。    

 │ │      · 短记忆：SummaryBufferMemory 摘要缓冲，控制 token 消耗                

│ │      · 长记忆：RAG 检索增强生成，向量数据库存储与语义检索                  

 │ │      · 记忆压缩：历史对话自动摘要，平衡完整性与性能 

**markdown渲染**

markdown语法转化为html代码：词法分析->token数组->语法分析->ast->渲染成html标签结构

正则匹配各种样式，如标签，代码块，段落，再细分匹配斜体 粗体 链接 普通文本，

匹配完返回token流（ast语法树），匹配类型为各种情况的，标签，代码块，段落...，(递归)返回对应的html字符串，并进行**html特殊字符转义**，**防止恶意脚本注入（XSS），同时确保内容能正确显示而不是被浏览器解析为标签**

remark/rehype插件

**会话持久化**

自动保存到 localStorage，刷新页面不丢失

用 LangChain 替换了直接调 OpenAI SDK 的方案，LangChain 负责：**接模型、管记忆、绑工具调用、处理流式** —— 相当于 AI 对话的「中控调度层」

[src/lib/langchain/memory.ts](vscode-webview://11uikhd98hqjgsikq78ghbt0pi0s5o8b2ecruc0hu799ir412sn1/src/lib/langchain/memory.ts)

自己实现了一套 **SummaryBufferMemory** —— 当聊天记录累计超过 token 阈值（默认 4000），自动调用 LLM 把**旧消息压缩成摘要**，避免上下文超长：

```
旧消息 → LLM 摘要压缩 → "对话历史摘要：xxx" + 最近的原始消息
```

这样既保留了长期记忆，又不会超过模型 token 限制。

## 虾皮

**流量染色/灰度路由**架构

**核心就是不同的 Docker 容器（或 Pod）**，配合**流量路由规则**实现隔离

**分支代码 → 构建独立 Docker 镜像 → 部署独立 Pod → 网关根据流量标记路由到你专属的容器**

1. 在测试平台点击"选择 PFB-xxx"
2. 浏览器写入 Cookie: _pfb_env=xxx
3. 后续所有请求自动带上 Header: X-PFB-Env: xxx
4. 网关读取 Header → 路由到对应 Pod/容器

### 方案

#### 双技术栈

- 项目方案+纯函数层

- vue2react

- 微前端
- Veaury 
- Vuex Store与React组件间的通讯基于 React.useSyncExternalStore 封装

```js
src/views/wms-basic/storage-area-setting/outbound-staging-location/hybrid.ts文件

import reactContainerFactory from '@/components/react-container/reactContainerFactory';
import { FC, lazy, LazyExoticComponent } from 'react';

export const CheckingStagingCompFactory1️⃣ = reactContainerFactory({//用于将 React 组件包装成可以在 Vue 中使用的组件
  name: 'outboundstaging.checking',
});
export const checkingStagingComp = (): LazyExoticComponent<FC> =>//懒加载函数
  lazy(
    () =>
      import(
        '@/react-app/pages/wms-basic/storage-area-setting/outbound-staging-location/checking-staging'
      )
  );

vue文件中：
<component :is="component" v-bind="props"/>
component: reactContainerFactory1️⃣({name属性}), //一个vue组件，在里面去调用函数渲染react组件
props: { component: LazyExoticComponent }, //一个懒加载函数，import加载react组件文件   

1️⃣onMounted(() => {
  renderReactComponent2️⃣(
    propComponent4️⃣, //传入的懒加载函数LazyExoticComponent
    rootRef.value5️⃣, //要挂载的ref节点
    attrs //所带的属性
  )
});

2️⃣render(
  <Wrapper>3️⃣ //包装react组件的组件，包括国际化，组件库配置，store配置等
    <Com4️⃣ {...props} /> //执行懒加载函数生成的react组件和所带属性
  </Wrapper>,
  rootNode5️⃣ //挂载节点
)
```

#### 权限脚本cicd

**流程**：

1.双向匹配策略
从 PermissionKey 向下找 URL
从 API 调用向上找 PermissionKey
确保两种写法都能被识别
2.行号精确定位
利用 Git diff 的行号信息
通过行号直接定位到源码对应行
避免全文扫描，提升效率
3.多分隔符支持
支持 &、|、,、# 等分隔符
自动去重，避免重复上传

```js
确定用户和分支名
  ↓
Git Diff
  ↓
line-number.sh 解析
  ↓
diff 文件（内容改变的行号:代码内容）
  ↓
_getPermissonRes() diff文件逐行解析，正则匹配文件名、文件里的权限点、API URL
  ↓
文件结构数组（文件名、权限点、API URL）
[
  {
    fileName: 'src/api/task-center/reassessment-task.ts',
    permissions: [
      { permissionKey: 'PC.Inbound.Task.ReassessmentTask.View', line: '59' }
    ],
    apis: [
      { url: '/api/v2/apps/process/inbound/poqc/create_communication', line: '60' }
    ]
  }
]
  ↓
_handleNotePermission() 匹配
  ↓
将源文件根据行依次添加到数组进行匹配工作
权限-URL 配对列表 双向匹配：根据对应line值确定数组行位置 从 PermissionKey 向下找 URL，从 API 向上找 PermissionKey
  ↓
_formatSuccessList() 格式化权限列表 处理&｜，#特殊字符
  ↓
最终权限列表 [{be_key, url}, ...]
  ↓
batchUploadPermission() 上传
  ↓
使用用户邮箱生成JWT 根据分支名确定jira单号
发送HTTP请求 服务器绑定权限
  ↓
deleleDiffFile() 清理diff文件
```

#### AICR

.cursorrules声明项目背景、用例模板、API模式、命名规范等->全局生效，团队共享，强约束，适合标准化项目

#### *低代码

物料

### 亮点

#### **防抖搜索框**

通过防抖，AbortController方法解决时序问题

**通过防抖减少无效请求 → AbortController取消已发请求 → 把请求参数记录到响应结果里，响应回来和输入框里的内容比较，如果内容不相同，忽略过期响应，不依赖请求发送顺序，只认"谁对应现在的输入框**

注：

- 时序问题如果后端不配合或无法取消的请求：闭包保存最新关键词，请求序号递增
- AbortController取消请求，不再等待响应，释放浏览器资源，但是后端正常处理，要让后端"拒收旧包裹"，必须让后端知道哪个是最新的 —— 靠 MID hash值 / 递增 ID / WebSocket 单通道

#### webworker

## minivue

实现了响应式原理，computed,watch,diff算法，父子通信，插槽，provide/inject,虚拟dom->真实dom

```js
模板字符串//编译
    ↓
 词法分析
parse() → Token 序列
    ↓
 语法分析 → AST（含 type, tag, props, children 等）
 	↓
AST 优化转换（transform）
   ├── 文本合并（相邻静态文本合并）
   ├── 静态提升（hoistStatic）→ 提取到渲染函数外
   ├── 补丁标记（patchFlag）→ 标记动态部分
   └── 树结构扁平化
    ↓
codegen() → 生成 render 函数字符串
    ↓
new Function() → 可执行的 render 函数
    ↓
执行 render(_ctx, _cache) → 返回 VNode 树（虚拟 DOM）//运行
    ↓
patch(null, VNode)，di'f'f → 创建真实 DOM → 插入页面
    ↓
响应式变化 → 重新执行 render → 新 VNode
    ↓
patch(oldVNode, newVNode) → diff → 最小化更新真实 DOM
```

**解释**

ref底层也是reactive实现的，底层也是对象，不过ref包装的是基本数据类型，无法用proxy代理，就包装成对象并加了一个value属性，并在render渲染函数时去解包，就是套一层Proxy把value用get,set表示

computed,watch其实本质也是响应式，底层就是用了ReactiveEffect的响应式系统，收集依赖，执行依赖，一个是加了缓存（通过一个布尔值判断是否修改），一个异步批量更新，通过把依赖函数数组放到nextTick里在下一个事件循环里去统一执行，遍历 pre-flush 队列，执行所有 job，既提升了性能，又保证了更新的一致性

nextTick就是将回调放到下一个事件循环中去执行，因为要用到这次事件循环更新的DOM或数据，所以原理就是把回调放到Promise.then里在下一次事件循环时直接执行

一个组件说白了就是一个对象，它有很多属性，如props,emit,slots,parent,provide(parent ? parent.provides : {}原型链查找祖先)，很多个组件通过parent等属性连接起来，实现父子通信

