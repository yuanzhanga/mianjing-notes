双技术栈怎么避免同样的功能逻辑重复

​		**抽离框架无关的共享层**，只保留视图层差异，业务逻辑写成纯函数，不依赖框架

​		组件旧组件原文件修改，新文件react写，如果有必要复用重构vue代码为react

权限上传流水线的设计，背景，同时多个权限处理冲突，比如只改了标点符号无效变更，怎么处理，怎么防止消耗流水线，阶梯式上传权限

​		引入代码格式化工具从根源上减少“只改标点符号”的情况，在流水线真正执行前增加一个**预检阶段**

aicr出来的点是不对的怎么设计人工介入，通用性的错误怎么避免ai下次出现同类型的错误

diff算法，虚拟dom添加key有什么区别

实习阶段难度挑战大的工作：高亮，多线程，搜索框

并行请求多个接口（urls数组），最大并发数为maxNum,返回所有接口的结果（原顺序），任何一个接口失败，整体失败