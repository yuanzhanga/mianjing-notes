# 腾讯AI面经 — 2026.6.3

## 1. 为什么选前端，做了哪些准备

**为什么选前端：**
- 前端是离用户最近的技术层，所见即所得的反馈感强，成就感来得快
- 前端技术栈更新快、生态活跃，能持续接触到新工具和新范式（如AI+前端）
- 前端正在从"页面渲染"走向"全栈能力"，端侧大模型、WebGPU等技术让前端的边界不断拓展

**做的准备：**
- 系统学习HTML/CSS/JS三件套，深入Vue3框架原理（响应式、虚拟DOM、插槽机制等）
- 关注AI+前端交叉领域，研究WebGPU + 端侧大模型（Transformers.js v4、WebLLM、BrowserAI）
- 实际项目经验：双技术栈开发、文件预览组件等业务实践
- 持续跟进AI Agent技术动态和扣子平台工作流开发

---

## 2. DOM、BOM有什么区别和联系

**DOM（Document Object Model）：**
- 文档对象模型，将HTML/XML文档映射为树形节点结构
- 核心对象：`document`
- 操作对象：页面中的元素（标签、属性、文本等）
- 标准由W3C制定

**BOM（Browser Object Model）：**
- 浏览器对象模型，提供与浏览器窗口交互的对象
- 核心对象：`window`
- 操作对象：浏览器环境（地址栏、历史记录、屏幕、定时器等）
- 没有统一标准，各浏览器自行实现（HTML5后逐步规范化）

**联系：**
- `document` 是 `window` 的属性，即 `window.document`，DOM是BOM的子集
- BOM提供运行环境，DOM在这个环境中操作文档内容
- 实际开发中常混合使用：`window.location`（BOM）获取URL，`document.getElementById`（DOM）操作元素

---

## 3. TCP的拥塞控制的原理，有哪些类型

**原理：**
拥塞控制是发送方根据网络拥塞程度动态调整发送速率的机制，防止过多数据注入网络导致拥塞。核心思想是"试探-调整"：先慢启动探测带宽，检测到拥塞则降速。

**四个核心算法：**

1. **慢启动（Slow Start）**
   - 初始拥塞窗口 cwnd = 1 MSS（最大报文段长度）
   - 每收到一个ACK，cwnd 翻倍增长（指数增长）
   - 达到慢启动阈值 ssthresh 后，切换到拥塞避免

2. **拥塞避免（Congestion Avoidance）**
   - 每个RTT，cwnd 增加 1 MSS（线性增长）
   - 增长速度远慢于慢启动，避免快速耗尽带宽

3. **快重传（Fast Retransmit）**
   - 收到3个重复ACK，立即重传丢失报文，不等超时定时器
   - 比超时重传更快响应丢包

4. **快恢复（Fast Recovery）**
   - 快重传后，将 ssthresh 设为 cwnd/2，cwnd 设为 ssthresh
   - 直接进入拥塞避免阶段，而非从慢启动重新开始

**拥塞事件后的处理：**
- **超时丢包**：ssthresh = cwnd/2，cwnd = 1 MSS，重新慢启动（保守策略）
- **3次重复ACK**：ssthresh = cwnd/2，cwnd = ssthresh，进入快恢复（认为网络还能传输部分数据）

---

## 4. 浏览器缓存策略流程

浏览器缓存分为强缓存和协商缓存，优先走强缓存，命中失败再走协商缓存。

**完整流程：**

1. 浏览器发起请求，先查本地缓存
2. 检查强缓存字段：
   - **Cache-Control**（HTTP/1.1，优先级更高）：
     - `max-age=秒`：缓存有效时长
     - `no-cache`：跳过强缓存，直接走协商缓存
     - `no-store`：完全禁止缓存
     - `private`/`public`：是否允许中间代理缓存
   - **Expires**（HTTP/1.0）：绝对过期时间，受客户端时钟影响，已被Cache-Control取代
3. 强缓存命中（未过期）→ 直接使用本地缓存，返回200（from cache），不发送请求
4. 强缓存未命中 → 向服务器发请求，走协商缓存
5. 协商缓存字段：
   - **Last-Modified / If-Modified-Since**：基于文件修改时间
   - **ETag / If-None-Match**：基于内容哈希（优先级更高，更精确）
6. 协商缓存命中 → 服务器返回304 Not Modified，浏览器使用本地缓存
7. 协商缓存未命中 → 服务器返回200 + 新资源

**缓存位置优先级：**
Service Worker → Memory Cache → Disk Cache → Push Cache（HTTP/2）

---

## 5. HTTP/2做了哪些优化

1. **多路复用（Multiplexing）**
   - 在同一个TCP连接上并行传输多个请求/响应，用帧（Frame）和流（Stream）标识归属
   - 彻底解决HTTP/1.1的队头阻塞问题（同一连接只能串行处理）
   - 不再需要雪碧图、域名分片等HTTP/1.1时代的hack方案

2. **头部压缩（HPACK）**
   - 使用静态表、动态表和哈夫曼编码压缩请求/响应头
   - 重复的Header字段（如Cookie、User-Agent）只需传索引号
   - 显著减少头部传输开销，尤其对API密集请求场景

3. **服务端推送（Server Push）**
   - 服务器可以主动推送资源到客户端缓存，无需等客户端请求
   - 典型场景：请求HTML时，服务端主动推送CSS和JS

4. **二进制分帧（Binary Framing）**
   - HTTP/1.1是文本协议，HTTP/2改为二进制格式
   - 数据被分割为更小的帧（Frame），解析效率更高，不易出错

5. **流优先级（Stream Priority）**
   - 客户端可为每个流设置权重和依赖关系
   - 保证关键资源（如CSS）优先于次要资源（如图片）传输

6. **流量控制（Flow Control）**
   - 基于流的粒度进行流量控制，类似TCP但作用在HTTP层

**注意：HTTP/2仍存在TCP层的队头阻塞**（丢包时所有流都受影响），这也是HTTP/3改用QUIC（基于UDP）的原因。

---

## 6. 最近的项目采用什么技术栈，什么架构

**技术栈：**
- 前端框架：Vue3 + TypeScript
- 构建工具：Vite
- UI组件库：Element Plus / Ant Design Vue
- 状态管理：Pinia
- 路由：Vue Router 4

**架构：**
- 采用组件化 + 模块化架构，按业务功能划分模块
- 公共逻辑抽取为Composables（组合式函数），实现逻辑复用
- API层统一封装，请求/响应拦截器处理鉴权和错误
- 双技术栈场景下（Vue + React），通过工具函数层实现跨框架逻辑复用

---

## 7. 双技术栈怎么解决函数复用

**核心思路：将业务逻辑与框架视图层解耦，提取为框架无关的纯函数层。**

1. **纯工具函数层**
   - 通用数据处理、格式化、校验等纯函数，无框架依赖
   - 两个框架项目直接引用同一份代码

2. **Monorepo 管理**
   - 使用 pnpm workspace 或 Turborepo 管理多包
   - `packages/shared/`：存放共享逻辑、类型定义、常量
   - `packages/vue-app/` 和 `packages/react-app/`：各框架实现

3. **Composable ↔ Hook 适配**
   - Vue的Composable和React的Hook理念相似但API不同
   - 共享逻辑写成纯函数，Vue侧用`ref`/`computed`包装为Composable，React侧用`useState`/`useMemo`包装为Hook
   - 示例：文件解析逻辑 → `useFileParser.ts`（共享核心） → `useFileParser.vue`（Vue包装） + `useFileParser.react.ts`（React包装）

4. **类型共享**
   - TypeScript接口、类型定义放在shared包，双端共用

5. **状态管理**
   - 复杂状态可用Zustand（React）和Pinia（Vue），共享初始状态和action逻辑

---

## 8. 实现多种文件格式的文件预览组件，实现思路和性能优化

**实现思路：**

1. **文件类型识别与路由**
   - 根据文件后缀/MIME类型，路由到对应解析器
   - 常见格式分类：
     - 文档类：PDF（pdf.js）、Word/Excel/PPT（转PDF或用对应SDK）
     - 图片类：原生`<img>`或Canvas渲染
     - 视频类：`<video>`标签 + HLS等流媒体协议
     - 文本类：代码高亮（Monaco Editor / Prism.js）
     - CAD/3D：Three.js或专用渲染器

2. **统一渲染接口**
   - 定义标准接口：`parse(file) → renderData`，每种格式实现自己的Parser
   - 策略模式：Parser注册表 + 动态加载

**性能优化：**

3. **异步解析**
   - 文件解析放入Web Worker，避免阻塞主线程
   - 大文件分块解析，先展示可用的部分内容

4. **流式转换**
   - 大文件不等待完整加载，边下载边解析边渲染
   - 使用ReadableStream处理流式数据，逐步呈现

5. **分片上传**
   - 大文件切片上传（如5MB一片），支持断点续传
   - 使用File.slice()切割，并发上传分片，服务端合并
   - 计算文件hash用于秒传判断和分片校验

6. **懒加载与按需引入**
   - 各格式解析器按需动态import，不打包到首屏
   - 预览组件懒加载，用户点击时才初始化

7. **缓存策略**
   - 解析结果缓存到IndexedDB，二次打开直接复用
   - 使用Service Worker缓存已下载的文件分片

---

## 9. 文件特殊格式或加密导致预览失败的兜底操作

1. **格式检测与降级渲染**
   - 优先尝试原始格式解析，失败后降级：如Excel → CSV纯文本展示
   - 服务端转码兜底：调用后端转换服务，将特殊格式转为PDF/图片再预览

2. **友好的用户提示**
   - 明确告知用户：文件格式不受支持 / 文件已加密，无法在线预览
   - 提供替代操作：下载到本地查看、使用专用软件打开

3. **加密文件处理**
   - 检测到加密标识后，弹窗提示用户输入密码
   - 调用对应解密库（如mutool for PDF）尝试解密后预览
   - 解密失败则提示"密码错误或文件损坏"，引导下载

4. **错误边界与监控**
   - 预览组件设置ErrorBoundary，捕获解析/渲染异常
   - 异常时展示兜底UI：文件图标 + 文件名 + 大小 + 操作按钮（下载/重试）
   - 上报异常日志（文件格式、错误信息），用于后续扩展支持的格式

5. **渐进增强**
   - 基础能力：所有文件都可下载
   - 增强能力：支持在线预览的格式逐步点亮预览按钮
   - 预览中显示加载进度条，超时自动降级为下载模式

---

## 10. 如何学习一门新技术

**方法论：熟悉新名词 → 问AI建立连接网**

1. **获取全局认知**
   - 先看官方文档的Introduction/Overview，了解技术定位、解决什么问题、核心概念
   - 遇到陌生名词先记录，不急着深挖

2. **新名词逐一击破**
   - 对每个新名词问AI/搜索："XX是什么？和YY有什么关系？"
   - 将新概念映射到已有知识体系上，如"WebGPU ≈ 浏览器版的GPU编程接口，类似WebGL但更底层"

3. **建立连接网**
   - 画思维导图或写笔记，把概念之间的关系串起来
   - 重点理解：这个技术依赖什么、被什么依赖、替代了什么、和什么互补

4. **动手验证**
   - 跑通官方最小demo，动手修改参数观察变化
   - 从"能跑"到"能改"再到"能写"

5. **项目驱动深化**
   - 用新技术做一个小项目，遇到问题再深入文档
   - 带着问题学习比通读文档效率高很多

6. **持续跟进**
   - 关注GitHub仓库、技术社区、相关博主
   - 订阅话题追踪，定期了解更新动态
