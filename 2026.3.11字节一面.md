#### 常用的数据类型

#### 怎么判断类型

**typeof 看原始类型，Array.isArray 看数组，Object.prototype.toString.call 看一切，instanceof 看原型继承关系。最稳妥的通用方案是 toString.call**

注：type of null=object;type of []=object

##### map,set,bigint介绍

特点，常用api,weakmap

get,set,delete,has,size,keys,values,entries

add,

JS 中 number 类型安全整数范围只有 -2^53 ~ 2^53**（约 ±9000 万亿），**BigInt 解决 number 精度上限问题，表示任意大整数，用 `n` 后缀或 `BigInt()` 创建。注意不能和 number 混算、JSON ，Math方法不支持、没有小数位

#### ES6常用语法

let,const,模板语法，箭头函数，扩展运算符，剩余参数，map/set,weakmap/weakset,proxy,reflect,class,promise

#### 模块规范

cjs,esm区别

**CJS 是运行时同步加载、值拷贝，适合服务端；ESM 是编译时静态分析、动态引用，支持 Tree Shaking，是浏览器和现代 Node 的标准。核心差异在加载时机和导出机制**

#### 浏览器宏任务，微任务

消息队列，事件循环，哪些是宏任务微任务

#### 模糊搜索场景题

1.频繁发请求

通过防抖，节流，AbortController方法

**通过防抖减少无效请求 → AbortController取消已发请求 → 把请求参数记录到响应结果里，响应回来和输入框里的内容比较，如果内容不相同，忽略过期响应**

2.时序问题

pomise.all -----串行比较慢

**不依赖请求发送顺序，只认"谁对应现在的输入框"**

#### 项目

##### vue中如何使用react

使用component组件

##### 两套技术栈有什么问题，如何解决

组件库，通用函数两份

通过函数将vue代码转化为jsx代码：

将模板代码转化为ast，怎么转化，正则匹配？

h函数实现

词法环境，语法环境怎么处理？

### webpack中es6代码如何转化为es5

 **babel-loader**--怎么实现？

解析-->转换-->生成

#### React中常用hooks

state,effect,memo,callback,ref

#### 怎么学习

#### 解释mcp,skills

#### 算法

1.千分位展示

倒序遍历加逗号

2.两个大数相加

各位相加进位